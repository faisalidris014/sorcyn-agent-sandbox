# Base URL of the Sorcyn backend API that the admin console proxies to.
# Server-side only (used by Route Handlers in app/api/* and lib/api.ts).
# Defaults to http://localhost:3000 if unset.
SORCYN_API_URL=http://localhost:3000
