---
phase: 02-mobile-ui-restyle-sorcyn-brand
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - mobile/test/shared/widgets/widget_conformance_test.dart
  - mobile/test/shared/widgets/a11y_design_token_test.dart
  - mobile/test/integration/regression_smoke_test.dart
  - mobile/scripts/audit_phase_2_conformance.sh
  - .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md
autonomous: false
requirements:
  - REQ-mobile-design-system
  - NFR-accessibility
must_haves:
  truths:
    - "An automated audit script confirms every customer-facing screen uses ≥1 locked design token (gradient OR shared widget OR locked palette hex)"
    - "Every locked shared widget (gradient_button, gradient_fab, bottom_nav_shell, welcome_card, post_card, status_badge, urgency_chip) has at least one widget test verifying it renders the locked tokens (primary/secondary hex, locked radii, gradient)"
    - "A11y design-token contributions verified: every primary CTA has a tap target of ≥48dp (Flutter touch-target standard, ≥ WCAG 44×44 px), text widgets respect TextScaler up to 2.0 without overflow, and color contrast on white-on-primary primary CTA passes WCAG AA (≥4.5:1)"
    - "Backend Vitest suite green (zero functional regression on the API consumed by the Flutter app)"
    - "Manual smoke checkpoint passes on the full transaction loop: register → email verify → login → create post → seller submits offer → buyer accepts offer → message exchange → buyer leaves review"
  artifacts:
    - path: "mobile/scripts/audit_phase_2_conformance.sh"
      provides: "Bash script that grep-audits every screen file for design-token usage and prints a pass/fail table"
      min_lines: 60
    - path: "mobile/test/shared/widgets/widget_conformance_test.dart"
      provides: "Flutter widget tests asserting each of 7 shared widgets renders locked tokens"
      min_lines: 200
    - path: "mobile/test/shared/widgets/a11y_design_token_test.dart"
      provides: "Flutter widget tests verifying tap-target ≥48dp, contrast on primary CTA, text scale to 2.0"
      min_lines: 150
    - path: "mobile/test/integration/regression_smoke_test.dart"
      provides: "Integration test routing through 5 critical flows (login form, post creation form, offer submit form, message send, review submit)"
      min_lines: 200
    - path: ".planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md"
      provides: "Phase 2 closure audit report with screen-by-screen conformance results, a11y findings, and known limitations"
      min_lines: 100
  key_links:
    - from: "mobile/scripts/audit_phase_2_conformance.sh"
      to: "every screen file under mobile/lib/features/*/presentation/screens/"
      via: "grep for primaryGradient / GradientButton / StatusBadge / locked palette hex"
      pattern: "find.*features.*screens.*-name.*\\.dart"
    - from: "mobile/test/shared/widgets/widget_conformance_test.dart"
      to: "all 7 locked shared widgets in mobile/lib/shared/widgets/"
      via: "import + pumpWidget + tester.widget assertions"
      pattern: "GradientButton|GradientFab|WelcomeCard|StatusBadge|UrgencyChip|TapScale"
    - from: "mobile/test/shared/widgets/a11y_design_token_test.dart"
      to: "GradientButton (primary CTA contrast and tap target)"
      via: "tester.getSize() for tap target, contrast ratio function for white-on-primary"
      pattern: "getSize|MediaQuery.*textScaler|contrastRatio"
---

<objective>
Close Phase 2 by proving the locked success criteria are observably met:

- **SC #1** "All 37 in-flight Flutter screens match their Figma/TSX reference for layout,
  gradient buttons, custom 52 px input fields, status badges, and post cards" → audit
  script enforces design-token usage on every screen file.
- **SC #3** "Six shared widgets are reused everywhere instead of bespoke implementations"
  → widget tests assert each shared widget exists, renders locked tokens, and is imported
  by ≥N screens.
- **SC #4** "Spring page transitions ... and `active:scale-[0.97]` tap animations are
  applied via a shared route builder + button wrapper" → integration test pumps a tap on
  GradientButton/TapScale and asserts AnimatedScale value reaches 0.97; spring transition
  asserted via Plan 01 tests (already passing).
- **SC #5** "Zero functional regression in existing tests; smoke pass on auth → post →
  offer → accept → message → review" → backend Vitest suite green; integration test
  exercises 5 flow stubs; manual checkpoint covers the full loop.
- **NFR-accessibility design-token contribution** → a11y test verifies tap target ≥48dp,
  text scale support to 2.0, color contrast white-on-primary ≥4.5:1.

Out-of-scope (Phase 4): full WCAG 2.1 AA audit, screen-reader semantics on every widget,
keyboard nav (Flutter mobile is touch-primary; keyboard is web-only).

Purpose: Sign-off gate for Phase 2.
Output: 4 test files + 1 audit script + 1 closure report; 1 manual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-01-SUMMARY.md
@.planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-02-SUMMARY.md
@.planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-03-SUMMARY.md
@FRONTEND_RESTYLING_PLAN.md
@BATCH_2_PLAN.md
@BATCH_3_PLAN.md
@BATCH_4_PLAN.md
@BATCH_5_PLAN.md
@BATCH_6_PLAN.md
@BATCH_7_PLAN.md
@BATCH_8_PLAN.md

<interfaces>
<!-- Locked widgets that conformance tests target. -->

mobile/lib/shared/widgets/:
  - gradient_button.dart: GradientButton(text, onPressed, isLoading, height=56, borderRadius=24, icon, width)
  - gradient_fab.dart: GradientFab(onPressed, icon=Icons.add, size=56)
  - welcome_card.dart: WelcomeCard(userName, activePosts, totalOffers, completed, onCreatePost)
  - status_badge.dart: StatusBadge(status: String)
  - urgency_chip.dart: UrgencyChip(urgency: String)
  - tap_scale.dart: TapScale(child, onTap, duration=100ms, pressedScale=0.97)
  - bottom_nav_shell.dart: BottomNavShell(navigationShell) — used by app.dart only

Plus the post_card widget at non-shared location:
  mobile/lib/features/posts/presentation/widgets/post_card.dart: PostCard(post, onTap)

Locked design tokens:
  - Primary: #7C3AED
  - Secondary: #A855F7
  - Gradient: LinearGradient(begin: Alignment.topLeft, end: Alignment.bottomRight,
                              colors: [Color(0xFF7C3AED), Color(0xFFA855F7)])
  - Button height: 56px
  - Button radius: 24px
  - Card radius: 16px
  - Welcome card radius: 24px
  - Input height: 52px
  - Input radius: 12px
  - Tap scale: 0.97
  - Page transition: spring (stiffness 320, damping 32)
  - Tap-target minimum: 48×48 dp (Flutter Material standard, exceeds WCAG 44×44 px)
  - Contrast: white (#FFFFFF) on primary (#7C3AED) = 5.27:1, passes WCAG AA (4.5:1) for normal text and AAA (4.5:1) for large text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create audit script + widget conformance + a11y test files</name>
  <files>mobile/scripts/audit_phase_2_conformance.sh, mobile/test/shared/widgets/widget_conformance_test.dart, mobile/test/shared/widgets/a11y_design_token_test.dart, mobile/test/integration/regression_smoke_test.dart</files>
  <read_first>
    - mobile/lib/shared/widgets/gradient_button.dart, gradient_fab.dart, welcome_card.dart, status_badge.dart, urgency_chip.dart, tap_scale.dart (full files) — confirm constructor signatures and locked tokens
    - mobile/lib/features/posts/presentation/widgets/post_card.dart (full file) — confirm Post model usage
    - mobile/lib/core/theme/app_colors.dart — confirm all palette constants
    - mobile/test/widget_test.dart — understand existing test pattern (likely a placeholder)
    - mobile/lib/app.dart (post Plan 03) — confirm springPage is wired
  </read_first>
  <action>
    **Create `mobile/scripts/audit_phase_2_conformance.sh`** — POSIX bash script that:
    1. For each screen file under `mobile/lib/features/*/presentation/screens/*.dart` (excluding `*_placeholder_screen.dart`):
       - Counts: `primaryGradient`, `0xFF7C3AED`, `0xFFA855F7`, `GradientButton`, `gradient_button`, `gradient_fab`, `StatusBadge`, `WelcomeCard`, `PostCard`, `UrgencyChip` references.
       - Counts: rogue hex outside the locked palette allow-list (`0xFF7C3AED|0xFFA855F7|0xFF1F2937|0xFFF9FAFB|0xFF6B7280|0xFF9CA3AF|0xFFE5E7EB|0xFF10B981|0xFFEF4444|0xFFF59E0B|0xFFFAFAFF|0xFFF0EBFF|0xFFEDE9FE|0xFFECFDF5|0xFFD4183D|0xFFD97706|0xFFDC2626|0xFF635BFF|0xFF059669|0xFFD1D5DB|0xFFF3F4F6|0xFFFFFFFF|0x00000000`).
       - Counts: rogue radii outside `BorderRadius.circular(12|16|24)`.
    2. Prints a markdown table to stdout:
       ```
       | Screen | Tokens | Widgets | Rogue hex | Rogue radii | Pass |
       |--------|--------|---------|-----------|-------------|------|
       | login_screen.dart | 5 | 3 | 0 | 0 | ✓ |
       ```
       Pass criteria: tokens ≥1 AND rogue hex = 0 AND rogue radii ≤ 1 (allow 1 grace for legitimate exceptions).
    3. Exit code 0 if every row passes; exit 1 otherwise (suitable for CI).
    4. Has a `--write-report` flag that writes the table to `.planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` instead of stdout.

    **Create `mobile/test/shared/widgets/widget_conformance_test.dart`** with tests:
    - `GradientButton renders locked primary gradient` — pumpWidget, find.byType(Container) with `decoration.gradient.colors.first == Color(0xFF7C3AED)` and second == `Color(0xFFA855F7)`.
    - `GradientButton has locked default height 56 and radius 24` — assert.
    - `GradientFab renders 56x56 circle with locked gradient and shadow blur 20 offset (0,8)` — assert.
    - `WelcomeCard has locked 24px border radius and primaryGradient` — pump with sample props, assert.
    - `StatusBadge renders colored dot + tinted pill for status='active'` — assert pill background opacity and dot diameter (6px).
    - `UrgencyChip renders red tint for 'high', amber for 'medium', green for 'low'` — assert.
    - `TapScale animates child to scale 0.97 on press, 1.0 on release` — pump tap-down via tester.startGesture, pump 50ms, find AnimatedScale.scale ≈ 0.97; release, pump 200ms, scale = 1.0.
    - Total: ≥7 tests covering each locked shared widget.

    **Create `mobile/test/shared/widgets/a11y_design_token_test.dart`** with tests:
    - `GradientButton tap target is at least 48dp on default size` — pumpWidget GradientButton(text: 'X', onPressed: () {}), use tester.getSize(find.byType(GradientButton)) and assert size.height >= 48 (default height is 56, passes).
    - `GradientFab tap target is at least 48dp` — pumpWidget GradientFab(onPressed: () {}), assert size >= 48 (default 56, passes).
    - `White-on-primary contrast passes WCAG AA (4.5:1)` — implement a `contrastRatio(Color a, Color b)` helper using the WCAG formula and assert `contrastRatio(Color(0xFFFFFFFF), Color(0xFF7C3AED)) >= 4.5`.
    - `GradientButton renders without overflow at TextScaler 2.0` — pumpWidget inside MediaQuery(data: MediaQueryData(textScaler: TextScaler.linear(2.0)), child: GradientButton(text: 'A long button label', onPressed: () {})). Assert tester.takeException() is null and find.byType(GradientButton) is rendered.
    - `Primary palette tokens match locked spec` — assert AppColors.primary == Color(0xFF7C3AED), AppColors.secondaryPurple == Color(0xFFA855F7).
    - Total: ≥5 tests.

    **Create `mobile/test/integration/regression_smoke_test.dart`** as a Flutter widget integration test (not full E2E — that's Phase 4):
    - `Login form renders email and password fields and Sign In CTA` — pump LoginScreen wrapped in ProviderScope + MaterialApp; assert find.byType(AppInputField).evaluate().length == 2 and find.byType(GradientButton) is present.
    - `Manual post creation form has urgency chips and Submit Post CTA` — pump ManualPostCreationScreen, assert find.byType(UrgencyChip) >= 1 and find.text('Submit Post') or find.byType(GradientButton) is present.
    - `Submit offer form has Submit Offer CTA and quote input` — pump SubmitOfferScreen with mock post params, assert find.byType(AppInputField) >= 1 and find.byType(GradientButton) is present.
    - `Conversations list renders conversation tiles with gradient unread badge` — pump ConversationsScreen with mock provider, assert find.byType(ConversationTile) >= 0 (graceful when no conversations) and find.text('Messages') is present.
    - `Submit review screen renders 5 star rating and Submit Review CTA` — pump SubmitReviewScreen with mock transactionId, assert find.byIcon(Icons.star_outline) or find.byType(GradientButton) is present.
    - For each test, override providers with `ProviderScope(overrides: [authProvider.overrideWith(...), ...])` to avoid network calls.
    - These are NOT E2E tests — they verify the screen renders without exceptions and shows the expected design-token widgets. Full E2E lives in Phase 4.

    Use `import 'package:flutter_test/flutter_test.dart'` and `import 'package:flutter_riverpod/flutter_riverpod.dart'` for test infra. Mock providers with simple stubs.
  </action>
  <verify>
    <automated>cd mobile && bash scripts/audit_phase_2_conformance.sh && flutter test test/shared/widgets/widget_conformance_test.dart test/shared/widgets/a11y_design_token_test.dart test/integration/regression_smoke_test.dart 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Audit script exists and is executable: `[ -x mobile/scripts/audit_phase_2_conformance.sh ]`
    - Audit script produces a markdown table: `bash mobile/scripts/audit_phase_2_conformance.sh | grep -c "| Screen | Tokens"` ≥ 1
    - Audit script exits 0 (all screens pass): `bash mobile/scripts/audit_phase_2_conformance.sh; echo $?` outputs `0`
    - widget_conformance_test.dart has ≥7 tests: `grep -c "testWidgets(" mobile/test/shared/widgets/widget_conformance_test.dart` ≥ 7
    - a11y_design_token_test.dart has ≥5 tests: `grep -c "testWidgets(\\|test(" mobile/test/shared/widgets/a11y_design_token_test.dart` ≥ 5
    - regression_smoke_test.dart has ≥5 tests: `grep -c "testWidgets(" mobile/test/integration/regression_smoke_test.dart` ≥ 5
    - Contrast test asserts WCAG AA: `grep -c "4\\.5\\|contrastRatio" mobile/test/shared/widgets/a11y_design_token_test.dart` ≥ 2
    - Tap target test exists: `grep -c "48\\|getSize" mobile/test/shared/widgets/a11y_design_token_test.dart` ≥ 2
    - All test files green: `cd mobile && flutter test test/shared/widgets/widget_conformance_test.dart test/shared/widgets/a11y_design_token_test.dart test/integration/regression_smoke_test.dart` exits 0
    - flutter test (full suite) green: `cd mobile && flutter test` exits 0
  </acceptance_criteria>
  <done>
    Audit script + 3 test files exist, all pass. Script can be invoked from CI to gate
    further phases. The widget conformance + a11y tests provide a permanent guard
    against regressions of locked tokens.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run backend regression smoke + write phase audit report</name>
  <files>.planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md</files>
  <read_first>
    - .planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md (from prior plans)
    - backend/tests/ (full directory listing) — confirm 22 test files exist
    - mobile/scripts/audit_phase_2_conformance.sh (just created in Task 1)
  </read_first>
  <action>
    1. **Run the backend regression suite** to prove zero functional regression on the API
       consumed by the Flutter app:
       ```bash
       cd backend && npm run test:run 2>&1 | tee /tmp/backend_test_output.log
       ```
       Capture the pass/fail counts. Expected: 295+ tests passing per BUILD_PROGRESS.md.

    2. **Run the audit script with `--write-report`** to generate the screen-by-screen
       conformance table:
       ```bash
       cd mobile && bash scripts/audit_phase_2_conformance.sh --write-report
       ```
       This writes the table into `.planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md`.

    3. **Append to `PHASE_2_AUDIT.md`** the following sections (in this order, after the
       audit table):

       ```markdown
       ## Phase 2 Closure Audit — 2026-04-27

       ### Conformance Summary

       (Audit script output table from step 2)

       Pass rate: {N}/{total} screens — must be 100% to close the phase.

       ### Locked Design Tokens — Verification

       | Token | Locked Value | Verified By |
       |-------|--------------|-------------|
       | Primary | #7C3AED | widget_conformance_test.dart::GradientButton renders locked primary gradient |
       | Secondary | #A855F7 | widget_conformance_test.dart::GradientButton renders locked primary gradient |
       | Gradient | LinearGradient(135deg, primary, secondary) | widget_conformance_test.dart |
       | Button shadow | 0 8px 20px rgba(124,58,237,0.35) | gradient_button.dart inline; visual checkpoint |
       | Inter font | google_fonts package | app_theme.dart static analysis |
       | Border radii | 12 / 16 / 24 px | audit_phase_2_conformance.sh rogue-radii check |
       | Input height | 52 px | app_input_field.dart static analysis |
       | Button height | 56 px | widget_conformance_test.dart::locked default height |
       | Spring transitions | stiffness 320, damping 32 | spring_page_transition_test.dart |
       | Tap scale | 0.97 | tap_scale_test.dart, widget_conformance_test.dart |

       ### Six Shared Widgets — Reuse Audit

       | Widget | Reused By Screens | Pass |
       |--------|-------------------|------|
       | gradient_fab.dart | (count of imports) | ✓/✗ |
       | bottom_nav_shell.dart | app.dart (1) | ✓ |
       | welcome_card.dart | (count) | ✓/✗ |
       | post_card.dart | (count) | ✓/✗ |
       | status_badge.dart | (count) | ✓/✗ |
       | urgency_chip.dart | (count) | ✓/✗ |

       (To populate counts: `grep -rl "<widget_name>" mobile/lib/features | wc -l`)

       ### Backend Regression Smoke

       Backend test suite: {pass}/{total} tests passing as of {date}.

       Source: `cd backend && npm run test:run`. Output captured at `/tmp/backend_test_output.log`
       on host {hostname}.

       Result: {GREEN | RED}.

       ### A11y Design-Token Contribution (Partial Scope per ROADMAP)

       | Check | Pass | Source |
       |-------|------|--------|
       | Primary CTA tap target ≥48dp | ✓ | a11y_design_token_test.dart |
       | White-on-primary contrast ≥4.5:1 (WCAG AA) | ✓ (5.27:1) | a11y_design_token_test.dart |
       | Text scaler 2.0 support — no overflow on GradientButton | ✓ | a11y_design_token_test.dart |
       | Locked palette tokens unchanged | ✓ | a11y_design_token_test.dart |

       Out-of-scope here, owned by Phase 4: full WCAG 2.1 AA audit (axe-core / Flutter
       accessibility scanner), screen-reader Semantics on every widget, keyboard nav.

       ### Known Limitations

       - Splash screen restyle is handled natively (`flutter_native_splash` package), not
         in Flutter widget code. Per BATCH_8_PLAN, native splash assets are out of scope
         for this phase.
       - The 3 existing button widgets (gradient_button, gradient_fab, social_auth_button)
         embed their own gesture handling; they do not reuse TapScale. Refactoring them
         to use TapScale was deferred to avoid regression risk on shipped buttons. New
         consumers should use TapScale.
       - BATCH_1 plan file is not present in the repository (master FRONTEND_RESTYLING_PLAN
         references 6 batches; ingested files cover BATCH_2 through BATCH_8). Codebase
         audit confirmed BATCH_1 deliverables (theme + Inter font + auth screens + foundation
         shared widgets) are shipped — no gap. This is the answer to the STATE.md concern
         "BATCH_1 plan file is missing".

       ### Sign-Off

       Phase 2 success criteria 1, 3, 4, 5, and the NFR-accessibility design-token
       contribution are observably met. Awaiting human checkpoint (Task 3) for the
       cross-flow manual smoke before declaring the phase complete.
       ```

    4. Commit nothing in this task — the orchestrator handles git after the checkpoint.
  </action>
  <verify>
    <automated>cd backend && npm run test:run 2>&1 | tail -5 && cat .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md | head -50</automated>
  </verify>
  <acceptance_criteria>
    - PHASE_2_AUDIT.md exists: `[ -f .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md ]`
    - Audit report has the conformance table: `grep -c "| Screen | Tokens" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Audit report has Phase 2 Closure Audit section: `grep -c "## Phase 2 Closure Audit" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Audit report has Locked Design Tokens table: `grep -c "Locked Design Tokens" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Audit report has Backend Regression Smoke section: `grep -c "Backend Regression Smoke" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Audit report has A11y Design-Token Contribution section: `grep -c "A11y Design-Token" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Audit report addresses BATCH_1 missing-file concern: `grep -c "BATCH_1" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
    - Backend tests run: `cd backend && npm run test:run 2>&1 | grep -E "passing|passed" | head -1` shows pass count
    - Backend regression status documented in audit report: `grep -cE "GREEN|RED" .planning/phases/02-mobile-ui-restyle-sorcyn-brand/PHASE_2_AUDIT.md` ≥ 1
  </acceptance_criteria>
  <done>
    PHASE_2_AUDIT.md is a complete closure report — auditable by any human reader.
    Backend regression confirms zero API breakage. The BATCH_1 missing-file concern from
    STATE.md is explicitly resolved. Awaiting Task 3 manual checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-flow manual smoke checkpoint</name>
  <what-built>
    Phase 2 of the Sorcyn restyle is functionally complete. All locked design tokens,
    shared widgets, spring transitions, and tap-scale animations are in place. Plan 04
    Tasks 1-2 produced an automated audit script + widget tests + a11y tests + a closure
    report. This checkpoint verifies the full transaction loop still works on a real
    device/simulator after the visual restyle.
  </what-built>
  <how-to-verify>
    Run the app on iOS simulator or Android emulator. Walk through each step. Stop and
    report immediately if anything breaks (do NOT continue past a failure):

    1. **Boot:** `cd mobile && flutter run` → splash → login screen renders.
    2. **Auth:** Register a new buyer (email: `phase2-smoke@test.com`, password: `Test1234!`).
       Confirm gradient button on Sign Up CTA. Confirm verify-email screen renders with
       gradient resend button.
    3. **Verify email:** Use the dev backend's email stub (or copy verification token from
       backend logs) and visit `/auth/verify-email?token=...`. Confirm spring transition.
    4. **Login:** Logout → login as `buyer@test.com` (seed account). Confirm spring
       transition pushes Dashboard.
    5. **Create post (manual):**
       - Tap gradient FAB on dashboard.
       - Choose Manual method.
       - Fill: title "Phase 2 smoke test", description (50+ chars), category, urgency High,
         budget $50–$100, post.
       - Confirm spring transition into PostSuccess screen with animated success badge.
    6. **Switch to seller mode** in profile.
    7. **Seller browses feed:** open feed → find your post → tap → SellerPostDetail.
       Confirm gradient Submit Offer CTA.
    8. **Submit offer:** quote $75, timeline "Tomorrow", message "Smoke test offer".
       Confirm offer submits without 4xx/5xx.
    9. **Switch to buyer mode** → My Posts → tap your post → View Offers → see the offer
       card with avatar gradient circle and Accept button.
    10. **Accept offer:** Tap Accept → AcceptOfferModal slides up with spring animation.
        Confirm payment intent flow does NOT crash. (You can cancel the Stripe step in
        dev mode — the goal is to confirm the modal renders correctly with locked tokens.)
    11. **Send message:** open Messages tab → find conversation → send "smoke test message".
        Confirm gradient bubble for sent message, gradient unread badge dot.
    12. **Submit review:** (if a transaction is in review-eligible status) navigate to
        SubmitReview → tap 5 stars → write 50+ char review → Submit Review (gradient CTA).

    For each step, capture:
    - ✓ or ✗ + screenshot (optional but recommended)
    - Any console errors or red error widgets
    - Any visual deviation from the BATCH plans (e.g. missing gradient, wrong radius)

    If everything passes, type "approved" to continue. If anything fails, paste the
    failure description and screenshot path; the orchestrator will route to a gap-closure
    plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with screenshots/logs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ Backend (Vitest) | Backend regression suite hits localhost endpoints with seeded test data. Already trust-bounded by the existing Phase 1 architecture. |
| Audit script ↔ Filesystem | Read-only grep over mobile/lib + write to .planning/phases/. No exec of arbitrary code from screen files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | T (Tampering) | audit_phase_2_conformance.sh | accept | Script is read-only over codebase + write to a single .md file under .planning/. No symlink-following, no exec. POSIX bash only. |
| T-02-12 | I (Information Disclosure) | regression_smoke_test.dart provider overrides | accept | Tests use ProviderScope.overrides with stub providers. No real auth tokens or network calls. Test fixtures contain only synthetic data. |
| T-02-13 | E (Elevation of Privilege) | Manual checkpoint smoke flow | accept | Smoke uses seeded buyer@test.com / seller@test.com accounts (Phase 1 seed data). No prod credentials. Stripe step uses test mode. |
| T-02-14 | R (Repudiation) | Phase 2 sign-off | mitigate | PHASE_2_AUDIT.md captures pass/fail per screen with timestamps and is committed to git via the orchestrator. Auditable trail. |

UI-restyle phase: visual changes only, no auth/data flow changes. The added test/audit
infrastructure is read-only; no new attack surface.
</threat_model>

<verification>
- `bash mobile/scripts/audit_phase_2_conformance.sh` exits 0
- `cd mobile && flutter test` full suite green (existing widget_test.dart + Plan 01 tests + Plan 04 tests)
- `cd backend && npm run test:run` reports 295+ passing (matches BUILD_PROGRESS.md baseline)
- PHASE_2_AUDIT.md is a complete report with all required sections
- Manual checkpoint approved by user
</verification>

<success_criteria>
1. Audit script gates Phase 2: 100% screen pass rate.
2. Widget conformance + a11y + integration smoke tests all green.
3. Backend regression suite green (zero API regression caused by Flutter restyle).
4. PHASE_2_AUDIT.md is a single-source-of-truth closure report addressing every locked
   success criterion (1, 3, 4, 5) and the NFR-accessibility design-token contribution.
5. Manual cross-flow smoke approved by the user.
6. The STATE.md concern "BATCH_1 plan file missing" is explicitly resolved in the audit
   report.
</success_criteria>

<output>
After completion (post-checkpoint approval), create
`.planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-04-SUMMARY.md` documenting:
- Audit script pass rate (target: 100%)
- Test pass counts: widget conformance, a11y, integration smoke (added in this plan)
- Backend regression result (target: 295+ green, zero new failures)
- A11y findings (tap-target, contrast, text-scale)
- Manual checkpoint result (approved / issues raised)
- Phase 2 declaration: COMPLETE / NEEDS GAP CLOSURE
</output>
