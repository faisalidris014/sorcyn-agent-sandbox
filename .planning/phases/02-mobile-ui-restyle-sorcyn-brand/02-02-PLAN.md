---
phase: 02-mobile-ui-restyle-sorcyn-brand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/settings/presentation/screens/settings_screen.dart
  - mobile/lib/features/settings/presentation/screens/help_support_screen.dart
  - mobile/lib/features/offers/presentation/screens/my_offers_screen.dart
  - mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart
  - mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart
autonomous: true
requirements:
  - REQ-mobile-design-system
must_haves:
  truths:
    - "settings_screen, help_support_screen, my_offers_screen, dispute_detail_screen, and seller_earnings_screen all use the locked Sorcyn primary gradient on at least one CTA or status accent"
    - "Each of the 5 screens uses one or more of the 6 locked shared widgets (gradient_fab, bottom_nav_shell with gradient indicator, welcome_card, post_card, status_badge, urgency_chip) where semantically appropriate, OR explicitly justifies in inline code comment why none apply"
    - "Each of the 5 screens uses GradientButton from shared/widgets for any primary CTA (no raw ElevatedButton with primary background as a workaround)"
    - "All hardcoded color values in the 5 screens reference AppColors constants or the locked palette hex (#7C3AED, #A855F7, plus the secondary palette in FRONTEND_RESTYLING_PLAN.md design token map). Rogue hex outside the locked palette → 0 occurrences."
    - "All hardcoded border radii in the 5 screens use 12 / 16 / 24 (locked tokens) — any other radius value triggers a comment justifying it"
    - "Functional behavior preserved: each screen still calls the same provider, navigates the same routes, and submits the same API payload as before"
  artifacts:
    - path: "mobile/lib/features/settings/presentation/screens/settings_screen.dart"
      provides: "Settings screen with primary CTA using GradientButton, gradient toggle tracks, locked radii"
      min_lines: 400
    - path: "mobile/lib/features/settings/presentation/screens/help_support_screen.dart"
      provides: "Help/Support screen with gradient quick-action cards, FAQ accordion using locked tokens"
      min_lines: 300
    - path: "mobile/lib/features/offers/presentation/screens/my_offers_screen.dart"
      provides: "My Offers screen — bring weak gradient usage (1 ref → ≥3 refs incl. primary CTA gradient)"
      min_lines: 300
    - path: "mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart"
      provides: "Dispute screen using StatusBadge widget, GradientButton for resolve/escalate"
      min_lines: 300
    - path: "mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart"
      provides: "Earnings screen with gradient balance card and GradientButton for payout request"
      min_lines: 400
  key_links:
    - from: "settings_screen.dart"
      to: "shared/widgets/gradient_button.dart"
      via: "import + usage on Save / primary CTA"
      pattern: "GradientButton\\("
    - from: "help_support_screen.dart"
      to: "shared/widgets/gradient_button.dart"
      via: "import + usage on Submit Contact / Send Message CTA"
      pattern: "GradientButton\\("
    - from: "my_offers_screen.dart"
      to: "shared/widgets/status_badge.dart"
      via: "import + usage on each offer card status"
      pattern: "StatusBadge\\("
    - from: "dispute_detail_screen.dart"
      to: "shared/widgets/status_badge.dart"
      via: "import + usage on dispute status pill"
      pattern: "StatusBadge\\("
    - from: "seller_earnings_screen.dart"
      to: "shared/widgets/gradient_button.dart"
      via: "import + usage on Request Payout CTA"
      pattern: "GradientButton\\("
---

<objective>
Close the per-screen conformance gap identified by the 2026-04-27 codebase audit.

The audit (`grep -c "primaryGradient|0xFF7C3AED|0xFFA855F7|gradient_button|GradientButton"`
over each screen file) found 5 screens with thin or zero gradient/shared-widget references
relative to their TSX design contract:

| Screen | Refs | TSX target |
|--------|------|------------|
| settings_screen.dart | 0 | SettingsScreen.tsx (gradient toggle tracks, primary CTA) |
| help_support_screen.dart | 0 | HelpSupportScreen.tsx (gradient quick-action cards) |
| my_offers_screen.dart | 1 | MyOffersScreen.tsx (gradient filter chips, status badges, accept CTA) |
| dispute_detail_screen.dart | 3 | DisputeDetailScreen.tsx (status banner, resolve gradient CTA) |
| seller_earnings_screen.dart | 2 | SellerEarningsScreen.tsx (gradient balance hero, payout CTA) |

These screens have inline-styled custom widgets (e.g. settings_screen has `_MenuCard`,
`_SectionLabel`) but lack the locked Sorcyn brand markers required by ROADMAP SC #1
("All 37 in-flight Flutter screens match their Figma/TSX reference for layout,
gradient buttons, custom 52 px input fields, status badges, and post cards") and SC #3
("Six shared widgets are reused everywhere instead of bespoke implementations").

This plan brings these 5 screens up to design-token conformance without touching
existing functionality (Riverpod, GoRouter, Dio calls preserved exactly).

Purpose: Close audit-identified gaps for SC #1 and SC #3.
Output: 5 screen files modified to use locked widgets/tokens; existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@FRONTEND_RESTYLING_PLAN.md
@BATCH_7_PLAN.md
@BATCH_5_PLAN.md
@BATCH_6_PLAN.md

<interfaces>
<!-- Locked shared widgets that the 5 screens must use. -->

From mobile/lib/shared/widgets/gradient_button.dart:
```dart
class GradientButton extends StatefulWidget {
  final String text;
  final VoidCallback? onPressed;
  final bool isLoading;
  final double height;       // default 56
  final double borderRadius; // default 24
  final IconData? icon;
  final double? width;

  const GradientButton({super.key, required this.text, this.onPressed,
    this.isLoading = false, this.height = 56, this.borderRadius = 24,
    this.icon, this.width});
}
```

From mobile/lib/shared/widgets/status_badge.dart (already exists):
```dart
class StatusBadge extends StatelessWidget {
  final String status; // e.g. 'active', 'pending', 'resolved', 'closed'
  // Renders a colored dot + tinted-pill text matching the status enum
}
```

From mobile/lib/core/theme/app_colors.dart:
```dart
class AppColors {
  static const Color primary = Color(0xFF7C3AED);
  static const Color secondaryPurple = Color(0xFFA855F7);
  static const Color background = Colors.white;
  static const Color surfaceVariant = Color(0xFFF9FAFB);
  static const Color border = Color(0xFFE5E7EB);
  static const Color black = Color(0xFF1F2937);
  static const Color grey = Color(0xFF6B7280);
  static const Color greyMedium = Color(0xFF9CA3AF);
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const LinearGradient primaryGradient = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [primary, secondaryPurple],
  );
}
```

From FRONTEND_RESTYLING_PLAN.md additional locked palette hex (allowed alongside AppColors):
- Foreground/Text: `#1F2937` (= AppColors.black)
- Card BG: `#F9FAFB` (= AppColors.surfaceVariant)
- Muted: `#6B7280` (= AppColors.grey)
- Grey medium: `#9CA3AF` (= AppColors.greyMedium)
- Border: `#E5E7EB` (= AppColors.border)
- Success: `#10B981` (= AppColors.success)
- Warning: `#F59E0B` (= AppColors.warning)
- Destructive: `#d4183d` or `#EF4444` (= AppColors.error)
- Info bg accents: `#FAFAFF`, `#F0EBFF`, `#EDE9FE`, `#ECFDF5` (purple/green tints — allowed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bring settings_screen.dart and help_support_screen.dart to gradient conformance</name>
  <files>mobile/lib/features/settings/presentation/screens/settings_screen.dart, mobile/lib/features/settings/presentation/screens/help_support_screen.dart</files>
  <read_first>
    - mobile/lib/features/settings/presentation/screens/settings_screen.dart (full file, ~647 lines) — preserves the existing `_MenuCard`, `_SectionLabel`, `_ChevronMenuItem`, `_DarkModeMenuItem`, etc. structures. Do not rewrite from scratch — patch in gradient/widget conformance only.
    - mobile/lib/features/settings/presentation/screens/help_support_screen.dart (full file, ~465 lines) — same: preserve `_faqs` const list and accordion structure.
    - designs/src/app/components/SettingsScreen.tsx — for the toggle gradient track, primary "Save" / "Sign Out" button styling
    - designs/src/app/components/HelpSupportScreen.tsx — for the quick-action card gradient, "Send Message" CTA gradient
    - mobile/lib/shared/widgets/gradient_button.dart — confirm constructor
    - mobile/lib/shared/widgets/section_card.dart — already used by help_support; confirm contract
  </read_first>
  <action>
    **settings_screen.dart changes (locked):**

    1. Replace any `ElevatedButton` or raw inline button used as a primary CTA (such as the "Sign Out" button if present, or any "Save Preferences") with `GradientButton(text: ..., onPressed: ...)`. If "Sign Out" should be a destructive outline button per the design, leave it but ensure styled per `error` palette and 24px border radius.

    2. The "Delete Account" button (currently rendered as a custom GestureDetector with `Color(0xFFEF4444).withValues(alpha: 0.07)` bg) is correct for a destructive action — preserve it but replace the raw hex `Color(0xFFEF4444)` with `AppColors.error`. Border radius must be 12 or 16 (verify against current value; if it is anything else, change it to 16).

    3. The custom toggle widget (`_DarkModeMenuItem`, the push/email switches) must use `AppColors.primaryGradient` for the active track. If currently using flat purple, change `decoration: BoxDecoration(color: AppColors.primary, ...)` on the active track to `decoration: BoxDecoration(gradient: AppColors.primaryGradient, ...)`. Toggle dimensions: 44×26 px (locked per BATCH_7_PLAN).

    4. All `_MenuCard` and section card border radii must be 16 (locked card token). Any other radius → change to 16 or document inline why.

    5. Top-level header "Settings" must use 22px w900 font weight (locked per BATCH_7_PLAN).

    **help_support_screen.dart changes (locked):**

    1. Replace any "Send Message" / "Submit" CTA at the bottom of the contact form with `GradientButton(text: 'Send Message', onPressed: _submit)`. If no submit CTA exists yet, ADD one — the design shows it.

    2. The 3 quick-action cards (Contact Us / FAQs / Report Bug) must each have a 4px gradient accent strip on the left or top, using `AppColors.primaryGradient`. Implementation: wrap each card with a Row that has a 4px-wide Container with `gradient: AppColors.primaryGradient` on the left.

    3. FAQ accordion: the active (expanded) item's left accent or chevron icon must use `AppColors.primary` color. The chevron rotates 180° when expanded (use `AnimatedRotation` with 200ms duration).

    4. Search bar (the `_searchController` input) must use the locked input radius: 14px per BATCH_7_PLAN, surfaceVariant fill, 1.5px border.

    5. Card border radii: 16px throughout (locked).

    **For BOTH screens, preserve:**
    - All Riverpod providers (auth_provider for settings, no providers for help_support)
    - All GoRouter `context.push` / `launchUrl` calls
    - All form state, controllers, validators
    - Existing semantics (no removed Semantics widgets, no changed labels — accessibility-relevant)
    - Existing AppLocalizations strings (do not hardcode English where loc strings exist)

    **Do NOT touch any other file.**
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/settings/presentation/screens/settings_screen.dart lib/features/settings/presentation/screens/help_support_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - settings_screen has GradientButton: `grep -c "GradientButton\\|gradient_button" mobile/lib/features/settings/presentation/screens/settings_screen.dart` ≥ 1
    - settings_screen has primaryGradient on toggle: `grep -c "primaryGradient" mobile/lib/features/settings/presentation/screens/settings_screen.dart` ≥ 2
    - help_support_screen has GradientButton CTA: `grep -c "GradientButton" mobile/lib/features/settings/presentation/screens/help_support_screen.dart` ≥ 1
    - help_support_screen has primaryGradient on quick-action accent: `grep -c "primaryGradient" mobile/lib/features/settings/presentation/screens/help_support_screen.dart` ≥ 1
    - No rogue hex (excluding palette comments): `grep -E "0xFF[0-9A-Fa-f]{6}" mobile/lib/features/settings/presentation/screens/settings_screen.dart | grep -v -E "0xFF7C3AED|0xFFA855F7|0xFF1F2937|0xFFF9FAFB|0xFF6B7280|0xFF9CA3AF|0xFFE5E7EB|0xFF10B981|0xFFEF4444|0xFFF59E0B|0xFFFAFAFF|0xFFF0EBFF|0xFFEDE9FE|0xFFECFDF5|0xFFD4183D|0xFFD97706|0xFFDC2626|0xFF635BFF|0xFF059669|0xFFD1D5DB|0xFFF3F4F6" | grep -v "^[[:space:]]*//" | wc -l | tr -d ' '` = 0
    - flutter analyze 0 errors on both files
    - flutter test (full suite) passes — `cd mobile && flutter test` exits 0
    - Functional preservation grep: `grep -c "context.push\\|context.go\\|launchUrl\\|ref\\.read\\|ref\\.watch" mobile/lib/features/settings/presentation/screens/settings_screen.dart` ≥ the count BEFORE the change (capture pre-count via `git show HEAD:mobile/.../settings_screen.dart | grep -c ...` and compare)
  </acceptance_criteria>
  <done>
    Both settings and help_support screens render with locked Sorcyn tokens (gradient on
    primary CTA, gradient toggle track, locked radii, locked palette only). All providers/
    routes/locale keys preserved. flutter analyze + test still green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bring my_offers_screen, dispute_detail_screen, and seller_earnings_screen to conformance</name>
  <files>mobile/lib/features/offers/presentation/screens/my_offers_screen.dart, mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart, mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart</files>
  <read_first>
    - mobile/lib/features/offers/presentation/screens/my_offers_screen.dart (full file, ~365 lines) — already has `_GradientFilterChip` inline; preserve and reuse
    - mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart (full file, ~504 lines)
    - mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart (full file, ~516 lines)
    - designs/src/app/components/MyOffersScreen.tsx
    - designs/src/app/components/DisputeDetailScreen.tsx
    - designs/src/app/components/SellerEarningsScreen.tsx
    - mobile/lib/shared/widgets/status_badge.dart — confirm StatusBadge constructor
    - mobile/lib/shared/widgets/gradient_button.dart — confirm constructor
  </read_first>
  <action>
    **my_offers_screen.dart changes (locked):**

    1. Each offer card's status indicator must use `StatusBadge(status: offer.status)` widget (imported from `shared/widgets/status_badge.dart`). If currently using a custom inline pill, replace with the shared widget.

    2. The "Withdraw" / "Start Work" / "View Details" action buttons on each card: the PRIMARY action ("Start Work" when accepted) must be `GradientButton(text: 'Start Work', onPressed: ..., height: 44, borderRadius: 12)`. Secondary actions stay as outline buttons.

    3. The header stats row uses `AppColors.primary` for the highlight numbers; the "Win Rate" stat must use `AppColors.success` per BATCH_6_PLAN.

    4. Filter chip: `_GradientFilterChip` already exists inline — confirm it uses `AppColors.primaryGradient` on selected. If not, fix.

    **dispute_detail_screen.dart changes (locked):**

    1. The status banner at top must use `StatusBadge(status: dispute.status)` widget for the status pill. The full-width red-tinted background banner stays but uses `AppColors.error.withValues(alpha: 0.08)` (not raw hex).

    2. The "Resolve" / "Escalate" action buttons: `Resolve` is the primary action and must use `GradientButton(text: 'Resolve', onPressed: ...)`. `Escalate` stays as an outline button.

    3. Evidence section card: 16px border radius, surfaceVariant background.

    4. Replace any raw hex outside the locked palette with AppColors constants.

    **seller_earnings_screen.dart changes (locked):**

    1. The "balance card" (the hero showing available balance) must have `decoration: BoxDecoration(gradient: AppColors.primaryGradient, borderRadius: BorderRadius.circular(24))` — locked welcome-card-style hero per BATCH_6_PLAN.

    2. "Request Payout" CTA must be `GradientButton(text: 'Request Payout', onPressed: ...)`.

    3. Earnings list rows: 60px height (locked per BATCH_6_PLAN payout row), 16px border radius for the row container background, status pill on each row uses `StatusBadge(status: payout.status)`.

    4. Fee breakdown section card: 16px radius, surfaceVariant fill.

    **For ALL THREE screens, preserve:**
    - All providers: `myOffersProvider`, `disputeProvider`, `earningsProvider` (etc.)
    - All `context.push`, `context.pop`, `context.go` calls
    - All Dio API calls
    - Form validation, controllers
    - AppLocalizations strings — do not introduce new hardcoded English where l10n keys already exist
    - Sentry error capture (if any)

    **Do NOT touch any other file.**
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/offers/presentation/screens/my_offers_screen.dart lib/features/transactions/presentation/screens/dispute_detail_screen.dart lib/features/sellers/presentation/screens/seller_earnings_screen.dart 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - my_offers_screen uses StatusBadge: `grep -c "StatusBadge\\(" mobile/lib/features/offers/presentation/screens/my_offers_screen.dart` ≥ 1
    - my_offers_screen uses GradientButton (primary action): `grep -c "GradientButton" mobile/lib/features/offers/presentation/screens/my_offers_screen.dart` ≥ 1
    - my_offers_screen has primaryGradient: `grep -c "primaryGradient" mobile/lib/features/offers/presentation/screens/my_offers_screen.dart` ≥ 2 (filter chip + button)
    - dispute_detail_screen uses StatusBadge: `grep -c "StatusBadge\\(" mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart` ≥ 1
    - dispute_detail_screen uses GradientButton: `grep -c "GradientButton" mobile/lib/features/transactions/presentation/screens/dispute_detail_screen.dart` ≥ 1
    - seller_earnings_screen has gradient balance card: `grep -c "gradient: AppColors.primaryGradient" mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart` ≥ 1
    - seller_earnings_screen has GradientButton: `grep -c "GradientButton" mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart` ≥ 1
    - seller_earnings_screen uses StatusBadge for payout rows: `grep -c "StatusBadge\\(" mobile/lib/features/sellers/presentation/screens/seller_earnings_screen.dart` ≥ 1
    - flutter analyze 0 errors across all 3 files
    - flutter test (full suite) green: `cd mobile && flutter test` exits 0
    - Provider preservation: `grep -c "myOffersProvider\\|disputeProvider\\|earningsProvider\\|ref\\.read\\|ref\\.watch" {each file}` ≥ the count BEFORE the change (verify via `git show HEAD:...`)
  </acceptance_criteria>
  <done>
    All 3 screens use the locked StatusBadge and GradientButton shared widgets,
    primaryGradient on hero/balance/filter, and locked radii/heights. Existing providers
    and navigation untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Visual restyle of 5 existing screens. No new auth flow, no new API endpoints, no new data persistence. All Riverpod state flow, GoRouter routes, and Dio API calls preserved by acceptance criteria. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T (Tampering) | settings_screen delete-account flow | accept | Visual restyle does not change the delete-account confirmation modal logic. The button still routes to the existing `_deleteAccount()` method which preserves the existing confirmation modal. |
| T-02-05 | I (Information Disclosure) | dispute_detail_screen evidence section | accept | Restyle changes only the container styling around evidence. Evidence URL fetching, signed-URL handling, and access checks are server-enforced and untouched. |
| T-02-06 | I | seller_earnings_screen balance display | accept | Earnings/payout amounts are read-only displays of provider state. No raw hex changes alter the data path. Currency formatting unchanged. |
| T-02-07 | E (Elevation of Privilege) | settings_screen toggle states | accept | Toggle changes still call the same `_updatePreferences()` method which uses the existing authenticated Dio client. Visual gradient track does not affect API authorization. |

UI-restyle phase: visual changes only, no auth/data flow changes (per security_threat_model_requirement).
</threat_model>

<verification>
- Per-file flutter analyze clean
- Full `flutter test` suite green (existing widget_test.dart still passes)
- Manual smoke check via `flutter run` on iOS simulator: open Settings → toggle dark mode (no functional change), open Help & Support → expand FAQ → confirm gradient accent, open My Offers → confirm filter chip + status badge, open Dispute Detail (test data) → confirm status banner, open Seller Earnings → confirm balance card gradient
- Functional invariants: `myOffersProvider`, `disputeProvider`, `earningsProvider`, `authProvider`, `notificationPreferences` API call all reachable in modified files (grep for symbol references)
- No new package added: `git diff mobile/pubspec.yaml` empty
</verification>

<success_criteria>
1. The 5 audit-flagged screens (settings, help_support, my_offers, dispute_detail, seller_earnings) all reach ≥1 GradientButton/StatusBadge usage where the design contract requires it.
2. Each of the 5 screens has ≥1 primaryGradient reference for hero/CTA/accent purposes.
3. Locked palette only: rogue hex outside the documented allow-list = 0 occurrences in all 5 files.
4. Locked radii (12 / 16 / 24) only — `grep -E "BorderRadius.circular\\([0-9]+\\)"` returns only 12, 16, or 24 in all 5 files.
5. flutter analyze + flutter test pass clean.
6. Provider/router/API call counts preserved (no functional regression).
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobile-ui-restyle-sorcyn-brand/02-02-SUMMARY.md` documenting:
- Per-screen list of changes (3-5 bullets each)
- Before/after grep counts for `primaryGradient`, `GradientButton`, `StatusBadge`
- Confirmation that no provider/router/API call was removed (with grep counts before/after)
- Any deviation from the design contract that was unavoidable (with rationale)
</output>
