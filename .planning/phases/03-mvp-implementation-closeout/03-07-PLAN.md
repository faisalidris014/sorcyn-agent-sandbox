---
phase: 03-mvp-implementation-closeout
plan: 07
type: execute
wave: 3
depends_on:
  - "03"
  - "05"
files_modified:
  - backend/src/common/utils/fees.ts
  - backend/src/common/utils/email-domain.ts
  - backend/src/modules/posts/posts.schemas.ts
  - backend/src/modules/posts/posts.service.ts
  - backend/src/modules/transactions/transactions.service.ts
  - backend/tests/fees.test.ts
  - backend/tests/transactions.test.ts
  - backend/tests/posts.test.ts
  - mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart
autonomous: true
requirements:
  - REQ-jobs-lead-gen
must_haves:
  truths:
    - "calculateJobLeadFee('entry') returns 10 (USD); calculateJobLeadFee('mid') returns 50; calculateJobLeadFee('specialized_senior') returns 500"
    - "When a transaction is created with transactionType='job_milestone' and the post's categorySpecific.roleTier is one of entry/mid/specialized_senior, the transaction's platform fee equals calculateJobLeadFee(roleTier)"
    - "Job posts created via the mobile manual-post flow include a roleTier dropdown (entry / mid / specialized_senior) when the category is Jobs"
    - "POST /api/v1/posts (Jobs category) from a free-email-domain user (gmail.com / yahoo.com / hotmail.com / outlook.com / aol.com / icloud.com / proton.me / protonmail.com) is rejected with HTTP 403 (or 400) by posts.service.createPost — the denylist is enforced at post creation, NOT at registration"
    - "Buyer-selected roleTier on job post creation is the source of truth (per Addendum A-05); Gemini-derived classification is deferred to Phase 6"
  artifacts:
    - path: "backend/src/common/utils/fees.ts"
      provides: "calculateJobLeadFee function"
      contains: "calculateJobLeadFee"
    - path: "backend/src/common/utils/email-domain.ts"
      provides: "isFreeEmailDomain + FREE_EMAIL_DOMAINS Set"
      contains: "FREE_EMAIL_DOMAINS"
    - path: "backend/src/modules/posts/posts.schemas.ts"
      provides: "categorySpecific.roleTier validation when category is Jobs"
      contains: "roleTier"
    - path: "backend/src/modules/transactions/transactions.service.ts"
      provides: "Lead-fee path when transactionType is job_milestone"
      contains: "calculateJobLeadFee"
    - path: "backend/src/modules/posts/posts.service.ts"
      provides: "Free-email denylist enforcement at createPost when category is Jobs"
      contains: "isFreeEmailDomain"
    - path: "mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart"
      provides: "Role-tier DropdownButtonFormField in Jobs branch"
      contains: "roleTier"
  key_links:
    - from: "Job post creation"
      to: "post.categorySpecific.roleTier"
      via: "manual post creation form Jobs branch"
      pattern: "DropdownButtonFormField"
    - from: "Transaction creation on job offer accept"
      to: "calculateJobLeadFee(post.categorySpecific.roleTier)"
      via: "transactions.service when transactionType==='job_milestone'"
      pattern: "calculateJobLeadFee"
    - from: "createPost (Jobs category)"
      to: "isFreeEmailDomain(user.email) gate"
      via: "posts.service.ts createPost free-email check"
      pattern: "isFreeEmailDomain"
---

<objective>
Deliver REQ-jobs-lead-gen: per-lead pricing engine ($10 / $50 / $500 by role tier per CLAUDE.md ranges) + employer email-domain denylist for job-poster post creation. Per Addendum A-05, role tier is **buyer-selected** from a dropdown (entry / mid / specialized_senior); Gemini-derived classification is deferred to Phase 6.

**Lead-fee plumbing:** the existing `transactions.service.ts` invokes `calculateFees()` (in `backend/src/common/utils/fees.ts`) when computing platform fees for a transaction. Add a sibling function `calculateJobLeadFee(roleTier)` and branch in transactions.service when `transactionType === 'job_milestone'` (the existing TransactionType enum value per CLAUDE.md): instead of the percentage-based fee, charge the flat lead fee. This is a behavioral change for jobs only; products + services + local fee paths are unchanged.

**Email-domain denylist gating — FIXED at post-creation:** the denylist is enforced at the **Jobs post-creation callsite** in `posts.service.ts createPost()`, NOT at registration. This is the chosen MVP path (no exploration of the auth-schema variant; the auth-schema path is rejected for this plan because it would require reasoning about job-poster intent at registration time, which isn't expressed in the current registration payload). Free-email users CAN still register and post Services / Products; only Jobs posts are blocked when the registrant's email is on the denylist.

**Sequencing note (depends_on: ["03", "05"])**: Plan 07 modifies `backend/src/modules/posts/posts.service.ts createPost()` (free-email gate) and `backend/tests/posts.test.ts` (new "Employer email-domain enforcement" describe). Plans 03 and 05 both also modify `posts.service.ts` and `posts.test.ts` — Plan 03 changes `getFeed()` and adds the "Targeted-seller carve-out" describe; Plan 05 changes `getPostById/getFeed/searchPosts` (PII redaction) and adds the "PII redaction" describe. Plan 07's edit lives in a different method (`createPost`) and a different describe block, so the file overlap resolves cleanly once 03 and 05 land first. Wave 3 ordering and explicit `depends_on` together prevent merge conflicts.

Purpose: Close REQ-jobs-lead-gen with the lead-pricing engine + employer email-domain check at post creation (Phase 3 Success Criterion 4).
Output: 9 files modified across backend (fees util + email-domain util + posts schema + posts.service createPost + transactions service + tests) + mobile (manual post creation form Jobs branch). The auth module is **not** touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md
@.planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md
@.planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md
@backend/src/common/utils/fees.ts
@backend/src/modules/posts/posts.schemas.ts
@backend/src/modules/posts/posts.service.ts
@backend/src/modules/transactions/transactions.service.ts
@mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart

<interfaces>
From backend/src/common/utils/fees.ts (existing pattern — sibling for new calculateJobLeadFee):
```typescript
// existing calculateFees(transactionType, amount, ...) — switch over TransactionType
// returns { platformFee: number, sellerNet: number, ... }
```

From CLAUDE.md fee section:
- Jobs: $10–500 per lead by role tier
- Entry: $10–20 (use $10), Mid: $30–50 (use $50), Specialized/Senior: $100–500 (use $500)

From RESEARCH.md "Don't Hand-Roll" → free-email denylist:
- gmail.com, yahoo.com, hotmail.com, outlook.com, aol.com, icloud.com, proton.me, protonmail.com (small initial set; admin can extend)

Existing categorySpecific JSON pattern (backend/src/modules/posts/posts.schemas.ts:49):
- categorySpecific is already a JSONB-backed loose object; roleTier slots in directly

Note (depends_on: ["03","05"]): post-Plan-03 and post-Plan-05 state of `backend/src/modules/posts/posts.service.ts`:
- `getFeed(query, requestContext, requestingUserId)` exists and includes the Plan 03 carve-out + Plan 05 batched PII redaction
- `getPostById` / `searchPosts` chain `redactPiiIfUnfunded` (Plan 05)
- `createPost` is UNTOUCHED by Plans 03 and 05 — Plan 07's free-email gate inserts cleanly inside `createPost` without colliding
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calculateJobLeadFee + transactions branch + posts schema roleTier validation</name>
  <files>backend/src/common/utils/fees.ts, backend/src/modules/posts/posts.schemas.ts, backend/src/modules/transactions/transactions.service.ts, backend/tests/fees.test.ts, backend/tests/transactions.test.ts</files>
  <read_first>
    - backend/src/common/utils/fees.ts (full file — confirm calculateFees switch shape, existing exports)
    - backend/src/modules/posts/posts.schemas.ts (full file — confirm categorySpecific shape; line 49 area)
    - backend/src/modules/transactions/transactions.service.ts (full file — find where calculateFees is invoked at transaction creation)
    - backend/prisma/schema.prisma (TransactionType enum — confirm value `job_milestone` exists or whatever the canonical Jobs value is)
    - backend/tests/transactions.test.ts (existing test patterns for transaction creation)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md sections "Job-creation flow — A-05 role-tier dropdown (mobile post creation)" + "Backend lead-pricing engine"
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Plan 6: Jobs lead-pricing engine" recommendations + "Phase Requirements REQ-jobs-lead-gen" row
  </read_first>
  <behavior>
    - Test 1: `calculateJobLeadFee('entry')` returns 10.
    - Test 2: `calculateJobLeadFee('mid')` returns 50.
    - Test 3: `calculateJobLeadFee('specialized_senior')` returns 500.
    - Test 4: TypeScript: `calculateJobLeadFee` typed argument is the union `'entry' | 'mid' | 'specialized_senior'`.
    - Test 5: Creating a transaction where `post.transactionType === 'job_milestone'` AND `post.categorySpecific.roleTier === 'mid'` results in a transaction with platformFee=50.
    - Test 6: posts.schemas.ts validates roleTier as one of three allowed values when present in categorySpecific. (Validation is informational — categorySpecific is already loose JSONB; the validation is a Zod sub-schema applied when `categoryId` is the Jobs category.)
  </behavior>
  <action>
    1. Open `backend/src/common/utils/fees.ts`. Add (do not replace existing exports):
       ```typescript
       export type JobRoleTier = 'entry' | 'mid' | 'specialized_senior';

       export function calculateJobLeadFee(roleTier: JobRoleTier): number {
         switch (roleTier) {
           case 'entry':              return 10;
           case 'mid':                return 50;
           case 'specialized_senior': return 500;
         }
       }
       ```
       (Exhaustive switch — TypeScript will error if a case is missed.)
    2. Open `backend/src/modules/posts/posts.schemas.ts`. Find the existing `categorySpecific` schema (around line 49). Add an optional `roleTier` validation:
       - Either inline as part of the existing schema: `categorySpecific: z.record(z.string(), z.any()).optional()` — keep loose. Then add a separate exported sub-schema `jobsCategorySpecificSchema = z.object({ roleTier: z.enum(['entry','mid','specialized_senior']).describe('Job role tier (Phase 3 A-05)'), ... }).passthrough()`.
       - At post creation in `posts.service.ts createPost()`, when the resolved category is Jobs, parse `input.categorySpecific` through `jobsCategorySpecificSchema` and reject with ValidationError if invalid. (Keep the change scoped — do not refactor the entire posts schema file.)
    3. Open `backend/src/modules/transactions/transactions.service.ts`. Find where `calculateFees(...)` is invoked at transaction creation. Add a branch:
       ```typescript
       if (post.transactionType === 'job_milestone') {
         const roleTier = (post.categorySpecific as { roleTier?: JobRoleTier })?.roleTier;
         if (!roleTier) throw new ValidationError('Job posts must specify a roleTier');
         platformFee = calculateJobLeadFee(roleTier);
         // sellerNet = price - platformFee  (or whatever the existing computation is — adapt)
       } else {
         // existing calculateFees() path
       }
       ```
       Adapt `platformFee` / `sellerNet` to match how the existing transactions.service.ts builds the transaction record. Read the file to confirm the shape; do not invent fields.
    4. Add tests:
       - `backend/tests/fees.test.ts` (extend if exists; create if not): three assertions for the three tiers.
       - `backend/tests/transactions.test.ts`: extend with a "Jobs lead fee" describe — create a Jobs post with categorySpecific.roleTier='mid', simulate offer-accept → transaction creation, assert platformFee=50.
    5. Run `cd backend && npx vitest run tests/fees.test.ts tests/transactions.test.ts` and confirm green.
  </action>
  <acceptance_criteria>
    - `grep -c "calculateJobLeadFee" backend/src/common/utils/fees.ts` returns `>=2` (function declaration + export reference if any)
    - `grep -c "JobRoleTier\|'entry'.*'mid'.*'specialized_senior'\|specialized_senior" backend/src/common/utils/fees.ts` returns `>=2`
    - `grep -c "calculateJobLeadFee" backend/src/modules/transactions/transactions.service.ts` returns `>=1`
    - `grep -c "job_milestone\|jobs.lead\|jobs_lead" backend/src/modules/transactions/transactions.service.ts` returns `>=1`
    - `grep -c "roleTier" backend/src/modules/posts/posts.schemas.ts` returns `>=1`
    - `grep -ic "calculateJobLeadFee\|jobs lead\|role.*tier" backend/tests/fees.test.ts backend/tests/transactions.test.ts` returns `>=4`
    - `cd backend && npx vitest run tests/fees.test.ts tests/transactions.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run tests/fees.test.ts tests/transactions.test.ts</automated>
  </verify>
  <done>calculateJobLeadFee added; transactions service branches on job_milestone to charge the flat fee; posts schema validates roleTier in Jobs branch; tests cover three tiers + integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Employer email-domain denylist enforcement at job-post creation (FIXED — post-creation gate, NOT auth)</name>
  <files>backend/src/common/utils/email-domain.ts, backend/src/modules/posts/posts.service.ts, backend/tests/posts.test.ts</files>
  <read_first>
    - backend/src/common/utils/ (full directory listing — find existing utils to co-locate the denylist with)
    - backend/src/modules/posts/posts.service.ts:60-130 (createPost method — find user lookup + category resolution; this is the only callsite for the denylist gate per the chosen MVP path)
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Don't Hand-Roll → Employer email-domain reject list" + "Phase Requirements REQ-jobs-lead-gen"
    - .planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md (REQ-jobs-lead-gen: "employer work-email domain verification (no Gmail)")
  </read_first>
  <behavior>
    - Test 1: `isFreeEmailDomain('alice@gmail.com')` returns true.
    - Test 2: `isFreeEmailDomain('alice@niftybyte.io')` returns false.
    - Test 3: Creating a post with categoryId=Jobs from a user whose email is `@gmail.com` is rejected with HTTP 403 (or 400) and a body containing 'work email' or 'employer email' (case-insensitive).
    - Test 4: Creating a post with categoryId=Services from a `@gmail.com` user succeeds (denylist applies only to Jobs).
    - Test 5: Creating a Jobs post from a `@niftybyte.io` user succeeds.
  </behavior>
  <action>
    1. Create `backend/src/common/utils/email-domain.ts`:
       ```typescript
       const FREE_EMAIL_DOMAINS = new Set([
         'gmail.com', 'yahoo.com', 'hotmail.com', 'outlook.com',
         'aol.com', 'icloud.com', 'proton.me', 'protonmail.com',
       ]);

       export function isFreeEmailDomain(email: string): boolean {
         const domain = email.toLowerCase().trim().split('@')[1];
         if (!domain) return false;
         return FREE_EMAIL_DOMAINS.has(domain);
       }

       export function getFreeEmailDomains(): readonly string[] {
         return Array.from(FREE_EMAIL_DOMAINS);
       }
       ```
    2. In `backend/src/modules/posts/posts.service.ts createPost()`:
       - After the user is loaded (or via a fresh `prisma.user.findUnique` lookup) and the category is resolved, check whether the resolved category is Jobs (compare against the Jobs umbrella category UUID — use the existing helper that resolves slugs, or check `category.slug === 'jobs'` if the categories table exposes slug).
       - If Jobs AND `isFreeEmailDomain(user.email)` is true, throw `new ForbiddenError('Job postings require a work email; free email providers are not supported')`.
       - This is the **only** path. Do NOT touch `backend/src/modules/auth/auth.schemas.ts` or `backend/src/modules/auth/auth.service.ts`. The denylist is NOT enforced at registration. Free-email users may register and post Services / Products freely — only Jobs posts are blocked. (Per W2: the optionality between "auth schema variant" vs "post-creation gate" is removed; only the post-creation gate is implemented.)
       - If unsure how categories are checked elsewhere in the service, replicate the existing pattern verbatim. Read the file before writing to identify the canonical "is this a Jobs category" check.
    3. Add tests:
       - `backend/tests/posts.test.ts` "Employer email-domain enforcement" describe — covers Tests 3, 4, 5 above.
       - Optionally add a unit test for `isFreeEmailDomain` directly (Tests 1 + 2) in a new `backend/tests/email-domain.test.ts` if no analog test file exists; otherwise extend an existing utility test.
    4. Run `cd backend && npx vitest run tests/posts.test.ts` (and the email-domain test if added) — confirm green.
  </action>
  <acceptance_criteria>
    - `test -f backend/src/common/utils/email-domain.ts` succeeds
    - `grep -c "FREE_EMAIL_DOMAINS\|isFreeEmailDomain" backend/src/common/utils/email-domain.ts` returns `>=3`
    - `grep -c "gmail.com\|yahoo.com\|hotmail.com" backend/src/common/utils/email-domain.ts` returns `>=3`
    - `grep -c "isFreeEmailDomain" backend/src/modules/posts/posts.service.ts` returns `>=1`
    - `grep -ic "work email\|employer email\|free email" backend/src/modules/posts/posts.service.ts` returns `>=1`
    - `grep -ic "free email\|work email\|gmail\|isFreeEmailDomain" backend/tests/posts.test.ts` returns `>=2`
    - **W2 (auth untouched):** `grep -c "isFreeEmailDomain\|FREE_EMAIL_DOMAINS" backend/src/modules/auth/auth.schemas.ts backend/src/modules/auth/auth.service.ts 2>/dev/null` returns `0`
    - `cd backend && npx vitest run tests/posts.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run tests/posts.test.ts</automated>
  </verify>
  <done>email-domain.ts utility shipped; createPost enforces the denylist for Jobs-category posts only (auth module untouched per W2); tests cover allow/deny behavior across category types.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile manual post creation Jobs-category roleTier dropdown</name>
  <files>mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart</files>
  <read_first>
    - mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart (full file — find Jobs branch + categorySpecific composition)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md section "Job-creation flow — A-05 role-tier dropdown (mobile post creation)"
    - mobile/lib/shared/widgets/ (find any existing Dropdown widget primitives)
  </read_first>
  <action>
    1. Open `manual_post_creation_screen.dart`. Locate the Jobs-category branch in the form (likely a conditional rendering of category-specific fields based on the resolved category slug or id).
    2. Add a `String? _roleTier;` state field at the State class top (near other form state).
    3. In the Jobs branch render block, add:
       ```dart
       const SizedBox(height: 16),
       DropdownButtonFormField<String>(
         decoration: const InputDecoration(labelText: 'Role Tier'),
         value: _roleTier,
         items: const [
           DropdownMenuItem(value: 'entry', child: Text('Entry')),
           DropdownMenuItem(value: 'mid', child: Text('Mid')),
           DropdownMenuItem(value: 'specialized_senior', child: Text('Specialized / Senior')),
         ],
         onChanged: (v) => setState(() => _roleTier = v),
         validator: (v) => v == null ? 'Required for job posts' : null,
       ),
       ```
       Use whatever existing Sorcyn-conformant input styling is used by the rest of the form (read the file for the pattern; if other DropdownButtonFormField uses already exist, mirror them).
    4. On submit, fold `_roleTier` into the `categorySpecific` map: `categorySpecific: { 'roleTier': _roleTier!, ...otherCategoryFields }`. Adapt to whatever the existing submit-handler structure looks like.
    5. Run `cd mobile && flutter analyze lib/features/posts/presentation/screens/manual_post_creation_screen.dart` — must pass with no errors.
  </action>
  <acceptance_criteria>
    - `grep -c "DropdownButtonFormField" mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart` returns `>=1`
    - `grep -c "'entry'\|'mid'\|'specialized_senior'" mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart` returns `>=3`
    - `grep -c "roleTier\|_roleTier" mobile/lib/features/posts/presentation/screens/manual_post_creation_screen.dart` returns `>=2`
    - `cd mobile && flutter analyze lib/features/posts/presentation/screens/manual_post_creation_screen.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/posts/presentation/screens/manual_post_creation_screen.dart</automated>
  </verify>
  <done>Role-tier dropdown rendered in Jobs branch; _roleTier folded into categorySpecific on submit; flutter analyze clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/v1/posts (Jobs category) | Untrusted user supplies roleTier — server validates against enum |
| client → POST /api/v1/transactions (job_milestone) | Server-side fee derivation from post.categorySpecific.roleTier — client cannot supply fee |
| client → POST /api/v1/posts (Jobs) from free-email account | Denied by isFreeEmailDomain check at post creation (NOT at registration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Job poster fakes a low role tier to pay smaller fee | mitigate | RoleTier is set at post-creation time and stored on the post; the transaction reads it from the authoritative post row, not from client input |
| T-03-07-02 | Spoofing | Free-email account masquerading as employer | mitigate | isFreeEmailDomain denylist enforced at Jobs post creation; ForbiddenError on match |
| T-03-07-03 | Information Disclosure | Denylist enumeration | accept | Denylist is public knowledge (well-known free providers); leaking it has no security impact |
| T-03-07-04 | Tampering | Admin extension of denylist | accept | Future enhancement: admin-managed list; for MVP the in-code Set is sufficient |
</threat_model>

<verification>
- Fees unit tests: 3 tier values
- Transaction integration test: lead fee path
- Posts service test: free-email denial for Jobs only (denylist enforced at createPost, NOT at register — auth module untouched)
- Mobile flutter analyze clean
</verification>

<success_criteria>
Phase 3 Success Criterion 4 fully delivered: per-lead pricing $10/$50/$500 by buyer-selected role tier; employer email-domain denylist rejects free-provider mail at Jobs-post creation. Gemini-classified tier deferred to Phase 6 per Addendum A-05.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mvp-implementation-closeout/03-07-SUMMARY.md` with:
- calculateJobLeadFee tier table
- Transactions service branch diff
- email-domain.ts denylist contents
- posts.service.ts createPost diff (free-email gate insertion point)
- Mobile dropdown diff
- Tests added
- Confirmation that auth.schemas.ts + auth.service.ts were NOT modified (per W2)
</output>
