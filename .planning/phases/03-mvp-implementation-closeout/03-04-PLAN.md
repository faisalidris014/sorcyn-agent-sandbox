---
phase: 03-mvp-implementation-closeout
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/src/modules/offers/offers.service.ts
  - backend/tests/offers.test.ts
autonomous: true
requirements:
  - REQ-counter-offer
must_haves:
  truths:
    - "A counter-offer chain reaches a maximum of 5 total rounds (original offer + 4 counters); the 5th counter (depth=4 attempt) is rejected with HTTP 409 ConflictError"
    - "The 4th counter (depth=3 → 4 total rounds) is accepted"
    - "Counter-offer privacy is enforced: a third-party seller GETting the post does NOT see the counter chain (verified via existing service-layer scoping)"
    - "The shipped Offer.parentOfferId self-relation chain and existing /offers/:id/counter endpoint remain canonical (Addendum A-02 — no new OfferRevision table)"
    - "The shipped mobile counter_offer_modal.dart (419 lines) is verified wired; no new modal file is created"
  artifacts:
    - path: "backend/src/modules/offers/offers.service.ts"
      provides: "MAX_COUNTER_DEPTH constant + depth-walk in counterOffer()"
      contains: "MAX_COUNTER_DEPTH"
    - path: "backend/tests/offers.test.ts"
      provides: "Round-cap depth test + privacy test"
      contains: "counter cap"
  key_links:
    - from: "offersService.counterOffer (depth check)"
      to: "Offer.parentOfferId chain walk via prisma.offer.findUnique"
      via: "while (cursor?.parentOfferId) { ... depth++ }"
      pattern: "parentOfferId"
---

<objective>
Implement the D-07 counter-offer round cap (5 total rounds = original + 4 counters) on the **shipped** `Offer.parentOfferId` self-relation chain (per Addendum A-02 — keep shipped chain, do NOT introduce new OfferRevision table). Verify D-08 privacy enforcement on the shipped chain.

This plan does NOT:
- Create a new `OfferRevision` table or schema migration
- Build a new mobile counter modal (the shipped 419-line `counter_offer_modal.dart` is canonical)
- Replace the existing `/offers/:id/counter` endpoint

Per RESEARCH.md Pitfall 6: before adding the cap, the executor MUST scan `backend/tests/offers.test.ts` for any existing test that creates >4 counter rounds — if found, update to expect 409 at the cap.

**Sequencing note (depends_on: ["01"])**: Plan 01 changes `MAX_OFFERS_PER_POST` (10 → 25) in this same `offers.service.ts` and edits `backend/tests/offers.test.ts`. This plan adds `MAX_COUNTER_DEPTH` adjacent to that constant and edits the same test file. Plan 01 must land first so this plan can stack its constant + tests cleanly without merge conflicts.

Purpose: Land the depth-cap business rule + privacy verification on the shipped surface, fulfilling Phase 3 Success Criterion 2.
Output: Depth check inside counterOffer() + cap test + privacy test extending offers.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md
@.planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md
@.planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md
@backend/src/modules/offers/offers.service.ts

<interfaces>
From backend/src/modules/offers/offers.service.ts (post-Plan-01):
```typescript
// near top of file
const MAX_OFFERS_PER_POST = 25; // Plan 01 — bumped from 10 to 25
// Plan 04 will add MAX_COUNTER_DEPTH = 4 immediately below this line.

// line 455 — counterOffer signature
async counterOffer(userId: string, offerId: string, input: CounterOfferInput) { ... }

// line 478 — atomic transaction creates new Offer with parentOfferId pointing to original
const [updatedOriginal, counterOffer] = await prisma.$transaction([
  // ... mark original as countered
  // ... create new Offer with parentOfferId: offerId
]);
```

From backend/src/common/utils/errors.ts:
- `ConflictError(message)` — used for business-rule violations (existing pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Counter-offer round-cap depth check + privacy verification</name>
  <files>backend/src/modules/offers/offers.service.ts, backend/tests/offers.test.ts</files>
  <read_first>
    - backend/src/modules/offers/offers.service.ts:455-545 (full counterOffer method body — confirm where the depth check must land BEFORE the prisma.$transaction)
    - backend/src/modules/offers/offers.service.ts (top of file: existing constants and imports — note `ConflictError` import location AND that Plan 01 has already landed `MAX_OFFERS_PER_POST = 25`)
    - backend/tests/offers.test.ts (full file — find any existing counter-offer tests; check if any creates >4 counter rounds, which would break after the cap lands; also confirm Plan 01's 26th-offer test exists so this plan does not stomp it)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md section "backend/src/modules/offers/offers.service.ts (line 21 const + counterOffer depth-cap)" for the exact depth-walk pattern
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Common Pitfalls 6" (counter-offer round cap conflicts)
    - .planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md Addendum A-02 (status of D-05/D-06/D-07/D-08/D-09/D-10)
  </read_first>
  <behavior>
    - Test 1: Submit original offer (depth=0). Buyer counters (depth=1, 2 total rounds). Seller counters (depth=2, 3 total). Buyer counters (depth=3, 4 total). Seller counters (depth=4, 5 total). Buyer attempts to counter again — assert 409 ConflictError with message containing "maximum" and "5" (or "rounds").
    - Test 2: Submit a 3-deep chain (4 total rounds); attempt one more counter — assert 200/201 success (5 total = at the cap, not over).
    - Test 3: Privacy: a third seller (not the offering seller) GETs the post via `GET /api/v1/posts/:id/offers` — assert the response does NOT include any offers from the counter chain (existing service scoping should already enforce this; the test is a verification, not a new feature). The privacy test name/describe MUST include text matching one of: "counter privacy", "third-party seller", "privacy counter", or "other seller" (so the W4 grep gate hits).
    - Test 4: The `MAX_COUNTER_DEPTH` constant is set to 4 (so 5 total rounds = original + 4 counters).
  </behavior>
  <action>
    1. Open `backend/src/modules/offers/offers.service.ts`. Add a constant immediately below the (Plan-01-updated) `MAX_OFFERS_PER_POST = 25` line near the top of the file:
       ```typescript
       const MAX_COUNTER_DEPTH = 4; // 5 total rounds = original + 4 counters (Phase 3 D-07)
       ```
    2. In `counterOffer()` (line 455), AFTER the existing load + auth checks but BEFORE the `prisma.$transaction([...])` block (around line 478), insert the depth check:
       ```typescript
       // Depth-cap check — walk parentOfferId chain (Addendum A-02 + D-07)
       let depth = 0;
       let cursor: { parentOfferId: string | null } | null =
         { parentOfferId: offer.parentOfferId ?? null };
       while (cursor?.parentOfferId) {
         const parent = await prisma.offer.findUnique({
           where: { id: cursor.parentOfferId },
           select: { parentOfferId: true },
         });
         depth++;
         cursor = parent;
       }
       if (depth >= MAX_COUNTER_DEPTH) {
         throw new ConflictError(
           `Counter-offer chain has reached the maximum of ${MAX_COUNTER_DEPTH + 1} rounds`,
         );
       }
       ```
       Adjust the initial `cursor` value to match how `offer` is loaded earlier in the method — if `offer.parentOfferId` is already in scope, use it; otherwise inspect lines 457-473 and adapt.
    3. Confirm `ConflictError` is imported (it should be — used elsewhere in the file). If not, add `ConflictError` to the imports from `'../../common/utils/errors.js'`.
    4. Open `backend/tests/offers.test.ts`. Add a new describe block "counter-offer round cap":
       - Setup: create buyer + seller test users via `createTestUser` helper. Create one buyer post. Submit one offer from seller.
       - Build the chain by alternating POST `/api/v1/offers/:id/counter` calls (buyer counters seller, seller counters buyer, etc.) — 4 successful counters total (depth 1 → 2 → 3 → 4). Assert each returns 201.
       - Submit a 5th counter — assert `res.statusCode === 409` and `JSON.stringify(res.json()).match(/maximum.*5.*rounds|maximum of 5/i)`.
    5. Add a separate test "counter-offer privacy — third-party seller cannot see other seller's counter chain":
       - Setup: buyer + sellerA + sellerB. Buyer post. SellerA submits offer; buyer counters sellerA's offer.
       - SellerB authenticates and calls `GET /api/v1/posts/:postId/offers`. Assert response does NOT include sellerA's counter chain (or returns only sellerB's offers). The exact endpoint shape is whatever exists in the routes — read `posts.routes.ts` or `offers.routes.ts` to confirm. If no listing endpoint exists for a third seller, instead assert sellerB's `GET /api/v1/offers/me` does not include sellerA's offers.
       - The describe / test names must contain at least one of the W4 grep tokens: `counter privacy`, `third-party seller`, `privacy counter`, or `other seller`.
    6. Run `cd backend && npx vitest run tests/offers.test.ts` — confirm green. If any pre-existing counter-offer test fails because it creates >4 counters, update that test to assert the cap.
    7. Verify the shipped mobile modal is wired (no code change, just grep verification — Plan 08 audit suite asserts this formally):
       - `grep -c "showCounterOfferModal" mobile/lib/features/offers/presentation/screens/offer_detail_screen.dart` should return `>=1`. Document this in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "MAX_COUNTER_DEPTH = 4" backend/src/modules/offers/offers.service.ts` returns `1`
    - `grep -c "parentOfferId" backend/src/modules/offers/offers.service.ts` returns `>=3` (the existing chain creation + the new depth walk references)
    - `grep -c "Counter-offer chain has reached the maximum" backend/src/modules/offers/offers.service.ts` returns `1`
    - `grep -ic "counter.*cap\|round cap\|MAX_COUNTER_DEPTH\|maximum of 5 rounds" backend/tests/offers.test.ts` returns `>=2`
    - `grep -ic "counter.*privacy\|third.party seller\|privacy.*counter\|other seller" backend/tests/offers.test.ts` returns `>=1` (W4 — privacy test acceptance)
    - `cd backend && npx vitest run tests/offers.test.ts` exits 0
    - SUMMARY.md documents the mobile-modal grep verification (`showCounterOfferModal` reference present in offer_detail_screen.dart)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run tests/offers.test.ts</automated>
  </verify>
  <done>MAX_COUNTER_DEPTH=4 constant defined adjacent to Plan-01's MAX_OFFERS_PER_POST=25; depth walk inserted before transaction in counterOffer(); 5th counter throws ConflictError; cap test + privacy test added (privacy test name/body contains W4 grep tokens); mobile modal verified wired (grep evidence in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/v1/offers/:id/counter | Either party (buyer or offering seller) can extend the chain; cap prevents runaway |
| client → GET /api/v1/posts/:id/offers | Third-party sellers MUST NOT see counter chain (D-08 privacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | Infinite counter chain | mitigate | Hard depth cap of 4 (5 total rounds) enforced in counterOffer service before any transaction commit |
| T-03-04-02 | Information Disclosure | Counter chain leaking to other sellers | mitigate | D-08 privacy: existing service-layer scoping (offers filtered by seller.id) confines visibility; verified by privacy test |
| T-03-04-03 | Tampering | Client supplies fake parentOfferId | mitigate | counterOffer service loads offer by id from DB (not client-supplied); chain walk uses authoritative parentOfferId from row |
</threat_model>

<verification>
- Round-cap depth test asserts 5th counter 409s
- Privacy test asserts third-party seller cannot see counter chain (test name/body matches W4 grep tokens)
- Mobile modal verified wired via grep (no code change needed)
- `cd backend && npx vitest run tests/offers.test.ts` green
</verification>

<success_criteria>
Phase 3 Success Criterion 2 fully delivered for the cap: counter-offer chain enforces 5-round maximum on the shipped Offer.parentOfferId chain. Privacy enforcement verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mvp-implementation-closeout/03-04-SUMMARY.md` with:
- MAX_COUNTER_DEPTH constant location
- counterOffer() diff (depth walk inserted before transaction)
- Two new test descriptions + their assertion shapes (cap test + privacy test)
- Mobile modal grep verification output
- Verification command output
</output>
