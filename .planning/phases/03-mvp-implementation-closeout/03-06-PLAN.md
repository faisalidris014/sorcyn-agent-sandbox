---
phase: 03-mvp-implementation-closeout
plan: 06
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - backend/src/config/env.ts
  - backend/src/modules/sellers/sellers.service.ts
  - backend/src/modules/sellers/sellers.routes.ts
  - backend/src/modules/payments/payments.service.ts
  - backend/src/modules/auth/auth.schemas.ts
  - backend/src/modules/auth/auth.service.ts
  - backend/prisma/schema.prisma
  - backend/tests/sellers.test.ts
  - backend/tests/auth.test.ts
  - mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart
  - mobile/lib/features/sellers/providers/seller_provider.dart
  - mobile/lib/features/sellers/data/repositories/seller_repository.dart
  - mobile/lib/features/auth/presentation/screens/register_screen.dart
  - mobile/lib/app.dart
autonomous: true
requirements:
  - REQ-verification-badges
  - REQ-business-account-ein
must_haves:
  truths:
    - "POST /api/v1/sellers/identity/verify creates a Stripe Identity VerificationSession with metadata.sellerProfileId and returns { sessionId, url }"
    - "Webhook handler for identity.verification_session.verified auto-creates a VerificationRequest with type='id', status='approved', sets sellerProfile.idVerified=true within a single prisma.$transaction"
    - "Webhook handler reuses the existing /api/v1/payments/webhook endpoint and the existing STRIPE_WEBHOOK_SECRET (no new env var per RESEARCH.md Pitfall 2)"
    - "auth/register rejects accountType=business without a valid EIN (XX-XXXXXXX format) with HTTP 400"
    - "Mobile IdentityVerifyScreen exists at mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart, uses url_launcher to open session.url, uses WidgetsBindingObserver to refetch profile on AppLifecycleState.resumed (per RESEARCH.md Pitfall 1 — flutter_stripe does NOT support Identity)"
    - "Mobile register screen reveals an EIN input field when accountType is set to 'business' and validates XX-XXXXXXX format client-side"
    - "Prisma migration `add_user_ein` is applied (not just generated) and User.ein column is queryable post-migration"
  artifacts:
    - path: "backend/src/modules/sellers/sellers.service.ts"
      provides: "createIdentitySession() method"
      contains: "stripe.identity.verificationSessions.create"
    - path: "backend/src/modules/payments/payments.service.ts"
      provides: "identity.verification_session.* webhook handlers"
      contains: "handleIdentitySessionVerified"
    - path: "backend/src/modules/auth/auth.schemas.ts"
      provides: "EIN field with regex + business-account superRefine"
      contains: "ein"
    - path: "mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart"
      provides: "Hosted-flow Identity verification screen"
      contains: "WidgetsBindingObserver"
    - path: "backend/prisma/migrations/*-add_user_ein/migration.sql"
      provides: "Applied Prisma migration adding User.ein column"
      contains: "ALTER TABLE"
  key_links:
    - from: "Mobile IdentityVerifyScreen"
      to: "POST /api/v1/sellers/identity/verify"
      via: "seller_provider.startIdentityVerification → repository → Dio"
      pattern: "startIdentityVerification"
    - from: "Stripe Identity webhook (verified event)"
      to: "VerificationRequest row with status='approved' + sellerProfile.idVerified=true"
      via: "handleWebhookEvent → handleIdentitySessionVerified"
      pattern: "identity.verification_session.verified"
    - from: "auth/register schema"
      to: "EIN regex validation"
      via: "superRefine cross-field check"
      pattern: "/^\\\\d{2}-\\\\d{7}$/"
---

<objective>
Deliver Phase 3 Success Criterion 5 in one plan: ID Verified badge auto-award via Stripe Identity webhook + EIN required at business registration. Per addendum decisions D-16, D-17, D-18 (KEEP, refined by RESEARCH.md):

- **D-16:** Webhook flow auto-creates approved `VerificationRequest` on `identity.verification_session.verified` event.
- **D-17:** Webhook reuses existing `/api/v1/payments/webhook` endpoint and existing `STRIPE_WEBHOOK_SECRET`. **No new env var** (overrides D-17's hedge per RESEARCH.md Pitfall 2 — Stripe assigns one signing secret per endpoint URL).
- **D-18:** Mobile uses **url_launcher hosted flow**, NOT flutter_stripe (per RESEARCH.md Pitfall 1 — issue #1819 still open as of 2026-04-29; flutter_stripe does not support Identity).

EIN gate: extends `registerSchema` with a discriminated requirement — when `accountType === 'business'`, EIN is required and must match `/^\d{2}-\d{7}$/`. Backend stores `ein` on the user (or seller_profile — verify schema first; existing `seller_profiles.einVerified` boolean is present at schema.prisma:291, suggests an EIN-bearing column may exist or needs adding).

Stripe Dashboard runbook step (manual, post-deploy): enable `identity.verification_session.verified`, `identity.verification_session.requires_input`, and `identity.verification_session.canceled` event types on the existing `/api/v1/payments/webhook` endpoint in Stripe Dashboard → Developers → Webhooks. Document this in SUMMARY.

This plan depends on Plan 02 because Plan 02 introduces `STRIPE_IDENTITY_RETURN_URL` patterns into env hardening; consolidating in Wave 3 keeps the Identity flow's env wiring consistent with Connect's Wave 1 hardening.

Purpose: Close REQ-verification-badges (ID Verified path) + REQ-business-account-ein in a single coherent verification plan.
Output: 14 files modified spanning backend (env + services + schema + tests) + mobile (new screen + provider/repo + register form + route registration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md
@.planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md
@.planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md
@backend/src/config/env.ts
@backend/src/modules/sellers/sellers.service.ts
@backend/src/modules/sellers/sellers.routes.ts
@backend/src/modules/payments/payments.service.ts
@backend/src/modules/payments/payments.webhook.ts
@backend/src/modules/auth/auth.schemas.ts
@backend/src/modules/auth/auth.service.ts
@backend/prisma/schema.prisma
@mobile/lib/features/sellers/presentation/screens/stripe_onboard_screen.dart
@mobile/lib/features/sellers/providers/seller_provider.dart
@mobile/lib/features/auth/presentation/screens/register_screen.dart
@mobile/lib/app.dart

<interfaces>
From backend/src/modules/payments/payments.service.ts:
```typescript
// line 266-289: handleWebhookEvent switch — TARGET FOR EXTENSION
async handleWebhookEvent(event: Stripe.Event) {
  switch (event.type) {
    case 'payment_intent.succeeded': ...
    case 'payment_intent.payment_failed': ...
    case 'charge.refunded': ...
    case 'account.updated': ...
    default: ...
  }
}
// line 380-390: handleAccountUpdated — analog for new handleIdentitySessionVerified
// line 205-237: startSellerOnboarding — analog for new createIdentitySession (different SDK namespace)
```

From backend/prisma/schema.prisma:
```
enum VerificationType { id ein license insurance background_check }  // line 165-171
model SellerProfile {
  idVerified  Boolean @default(false)  // line 291
  einVerified Boolean @default(false)  // line 292
  ...
}
model VerificationRequest {
  // (existing — confirm fields during execution)
}
model User {
  // existing fields — may or may not have an `ein` column. Verify and add if absent.
}
```

From backend/src/modules/auth/auth.schemas.ts:
```typescript
// line 14-32 — registerSchema (TARGET FOR EXTENSION)
export const registerSchema = z.object({
  ...
  accountType: z.enum(['buyer', 'seller', 'both']).default('buyer'),  // line 20
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Stripe Identity session + webhook + EIN registration gate</name>
  <files>backend/src/config/env.ts, backend/src/modules/sellers/sellers.service.ts, backend/src/modules/sellers/sellers.routes.ts, backend/src/modules/payments/payments.service.ts, backend/src/modules/auth/auth.schemas.ts, backend/src/modules/auth/auth.service.ts, backend/prisma/schema.prisma, backend/tests/sellers.test.ts, backend/tests/auth.test.ts</files>
  <read_first>
    - backend/src/modules/payments/payments.service.ts:1-20 (imports), 205-240 (startSellerOnboarding analog), 266-310 (handleWebhookEvent switch), 380-400 (handleAccountUpdated analog)
    - backend/src/modules/payments/payments.webhook.ts (full file — confirm signature verification + dedup pattern; do NOT touch)
    - backend/src/modules/sellers/sellers.service.ts (full file — find where to co-locate createIdentitySession, near submitVerification)
    - backend/src/modules/sellers/sellers.routes.ts (existing route pattern for app.authenticate)
    - backend/src/modules/auth/auth.schemas.ts:1-65 (registerSchema)
    - backend/src/modules/auth/auth.service.ts:60-110 (register method — where to add EIN persistence)
    - backend/prisma/schema.prisma — find User model fields; check if `ein` column exists; check VerificationRequest model fields exactly
    - backend/src/config/env.ts:50-55 (STRIPE_CONNECT_RETURN_URL pattern)
    - backend/tests/payments.test.ts:1-50 (Stripe mock pattern for tests)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md sections "createIdentitySession()", "handleWebhookEvent (extend switch)", "auth.service.ts + auth.schemas.ts (EIN gate at register)"
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Pitfall 1" + "Pitfall 2" + "Code Examples" sections
  </read_first>
  <behavior>
    - Test 1: POST /api/v1/sellers/identity/verify with a valid bearer token returns 201 + JSON `{ success: true, data: { sessionId: 'vs_...', url: 'https://verify.stripe.com/...' } }`. The Stripe SDK is mocked; assert `stripe.identity.verificationSessions.create` was called with `{ type: 'document', metadata: { sellerProfileId: <id>, userId: <id> }, ... }`.
    - Test 2: A simulated `identity.verification_session.verified` webhook event with `metadata.sellerProfileId` set causes a new VerificationRequest row with type='id', status='approved' AND sellerProfile.idVerified=true. The check is via `prisma.verificationRequest.findFirst` and `prisma.sellerProfile.findUnique`.
    - Test 3: A simulated `identity.verification_session.verified` event with NO metadata (sellerProfileId missing) is logged but does NOT create a VerificationRequest.
    - Test 4: POST /api/v1/auth/register with `{ accountType: 'business', /* no ein */, ... }` returns 400 with body containing 'EIN' or 'ein' (case-insensitive).
    - Test 5: POST /api/v1/auth/register with `{ accountType: 'business', ein: '12-3456789', ... }` returns 201 and the user row has `ein='12-3456789'` (or whichever column was added).
    - Test 6: POST /api/v1/auth/register with `{ accountType: 'buyer' /* no ein */ }` returns 201 (existing behavior preserved).
    - Test 7: POST /api/v1/auth/register with `{ accountType: 'business', ein: 'invalid' }` returns 400.
  </behavior>
  <action>
    1. **Env (env.ts):** Add `STRIPE_IDENTITY_RETURN_URL: z.string().min(1).default('reversemarket://seller/identity/complete'),` next to the Stripe Connect URL defaults from Plan 02. Do NOT add `STRIPE_IDENTITY_WEBHOOK_SECRET` (per Pitfall 2 — single secret per endpoint).
    2. **Schema (prisma/schema.prisma) — W1 migration apply:** Confirm User model has an `ein String?` field. Grep first: `grep -n "^\\s*ein\\b" backend/prisma/schema.prisma`. If absent, add to User model:
       ```
       ein String? @db.VarChar(10)
       ```
       Then run, in this exact order:
       ```bash
       cd backend && npx prisma format
       cd backend && npx prisma migrate dev --name add_user_ein
       ```
       The second command **applies** (not just generates) the migration against the local dev database. Verify the migration files exist on disk:
       ```bash
       ls backend/prisma/migrations/*-add_user_ein/
       ```
       This must list at least `migration.sql` inside a directory whose name matches `<timestamp>-add_user_ein`. Do NOT use `--create-only` — that would create the migration without applying it, leaving the DB and schema out of sync for the rest of the test run.
       If the project uses a custom-migration SQL workflow instead of Prisma's standard migrate flow (check for `backend/prisma/custom-migrations/` precedent), follow that workflow instead AND ensure the SQL is applied (not just authored) before tests run. Document the choice in SUMMARY.
    3. **sellers.service.ts:** Add new method `createIdentitySession(userId)` mirroring `paymentsService.startSellerOnboarding()` — see PATTERNS.md "createIdentitySession()" section for the exact body. Use `getStripe()` (lazy init), `stripe.identity.verificationSessions.create({ type: 'document', options: { document: { require_matching_selfie: true } }, metadata: { sellerProfileId, userId }, return_url: env.STRIPE_IDENTITY_RETURN_URL })`. Use `idempotencyKey: \`identity_session_${seller.id}\``.
    4. **sellers.routes.ts:** Register `POST /sellers/identity/verify` with `onRequest: [app.authenticate]`, calling `sellersService.createIdentitySession(request.user.sub)` and returning `reply.status(201).send({ success: true, data: result })`. Place near the existing `submitVerification` route. Match the existing schema/tag/security shape.
    5. **payments.service.ts handleWebhookEvent:** Extend the switch with three new cases AFTER `account.updated`:
       ```typescript
       case 'identity.verification_session.verified':
         await this.handleIdentitySessionVerified(event.data.object as Stripe.Identity.VerificationSession);
         break;
       case 'identity.verification_session.requires_input':
       case 'identity.verification_session.canceled':
         // Log only; user can retry hosted flow
         break;
       ```
       Add the private handler `handleIdentitySessionVerified` right after `handleAccountUpdated`:
       ```typescript
       private async handleIdentitySessionVerified(vs: Stripe.Identity.VerificationSession) {
         const sellerProfileId = vs.metadata?.sellerProfileId;
         if (!sellerProfileId) return;
         await prisma.$transaction(async (tx) => {
           await tx.verificationRequest.create({
             data: {
               sellerId: sellerProfileId,
               verificationType: 'id',
               tier: 2,
               status: 'approved',
               reviewedAt: new Date(),
               notes: `Auto-approved via Stripe Identity ${vs.id}`,
             },
           });
           await tx.sellerProfile.update({
             where: { id: sellerProfileId },
             data: { idVerified: true },
           });
         });
       }
       ```
       Adapt field names (`verificationType`, `tier`, `notes`, etc.) to match the actual VerificationRequest model — read schema.prisma to confirm. If a `tier` field doesn't exist, drop it; if `notes` is named differently, use the existing name.
    6. **auth.schemas.ts:** Extend registerSchema:
       - Add `'business'` to the `accountType` enum: `accountType: z.enum(['buyer', 'seller', 'both', 'business']).default('buyer')`.
       - Add an optional `ein` field: `ein: z.string().regex(/^\d{2}-\d{7}$/, 'Invalid EIN format (expected XX-XXXXXXX)').optional()`.
       - Wrap the schema in `.superRefine` (or `.refine`) to require EIN when accountType=business:
         ```typescript
         }).superRefine((data, ctx) => {
           if (data.accountType === 'business' && !data.ein) {
             ctx.addIssue({ code: 'custom', path: ['ein'], message: 'EIN is required for business accounts' });
           }
         });
         ```
       - Update the exported `RegisterInput` type accordingly.
    7. **auth.service.ts register():** Add `ein: input.ein ?? null` (or appropriate field name) to the `prisma.user.create({ data: ... })` block.
    8. **Tests:**
       - Extend `backend/tests/sellers.test.ts` with two new describes: "POST /sellers/identity/verify" and "Stripe Identity webhook handler". Mock `getStripe` per `tests/payments.test.ts` pattern; specifically mock `stripe.identity.verificationSessions.create` to return `{ id: 'vs_test', url: 'https://verify.stripe.com/test' }`. For the webhook test, call `paymentsService.handleWebhookEvent({ type: 'identity.verification_session.verified', data: { object: { id: 'vs_test', metadata: { sellerProfileId: <id> } } } } as any)` and then assert `prisma.verificationRequest.findFirst({ where: { sellerId, status: 'approved' } })` is non-null AND `prisma.sellerProfile.findUnique({ where: { id: sellerProfileId } }).idVerified === true`.
       - Extend `backend/tests/auth.test.ts` with "EIN gate" describe covering Tests 4–7.
    9. Run `cd backend && npx vitest run tests/sellers.test.ts tests/auth.test.ts tests/payments.test.ts` and confirm green.
    10. **Stripe Dashboard runbook step (manual, document only):** in SUMMARY, document that the operator must enable `identity.verification_session.verified`, `identity.verification_session.requires_input`, `identity.verification_session.canceled` event types on the existing `/api/v1/payments/webhook` endpoint in Stripe Dashboard → Developers → Webhooks. NO new endpoint. NO new signing secret.
  </action>
  <acceptance_criteria>
    - `grep -c "STRIPE_IDENTITY_RETURN_URL" backend/src/config/env.ts` returns `>=1`
    - `grep -c "STRIPE_IDENTITY_WEBHOOK_SECRET" backend/src/config/env.ts` returns `0` (per Pitfall 2)
    - `grep -c "createIdentitySession" backend/src/modules/sellers/sellers.service.ts` returns `>=1`
    - `grep -c "stripe.identity.verificationSessions.create" backend/src/modules/sellers/sellers.service.ts` returns `1`
    - `grep -c "/identity/verify" backend/src/modules/sellers/sellers.routes.ts` returns `>=1`
    - `grep -c "identity.verification_session.verified" backend/src/modules/payments/payments.service.ts` returns `>=1`
    - `grep -c "handleIdentitySessionVerified" backend/src/modules/payments/payments.service.ts` returns `>=2`
    - `grep -c "'business'" backend/src/modules/auth/auth.schemas.ts` returns `>=1`
    - `grep -c "EIN is required for business accounts\|EIN.*required\|/\\^\\\\d{2}-\\\\d{7}\\$/" backend/src/modules/auth/auth.schemas.ts` returns `>=1`
    - **W1 (migration apply):** `grep -ric "ein.*String\\?\\|ein  *String\\?" backend/prisma/schema.prisma` returns `>=1`
    - **W1 (migration apply):** `ls backend/prisma/migrations/*-add_user_ein/ 2>/dev/null` returns a non-empty directory listing (at minimum a `migration.sql` file). If the project uses the custom-migrations workflow instead, the SUMMARY documents that and the equivalent applied SQL file path is listed.
    - `cd backend && npx vitest run tests/sellers.test.ts tests/auth.test.ts tests/payments.test.ts` exits 0
    - SUMMARY documents the Stripe Dashboard runbook step
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run tests/sellers.test.ts tests/auth.test.ts tests/payments.test.ts</automated>
  </verify>
  <done>Backend Identity session creation endpoint + webhook handler shipped; EIN gate enforced at registration with format validation; User.ein column present in schema AND applied migration on disk under backend/prisma/migrations/*-add_user_ein/; tests cover happy + sad paths; Stripe Dashboard runbook documented.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile IdentityVerifyScreen + provider/repo + route registration + register form EIN field</name>
  <files>mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart, mobile/lib/features/sellers/providers/seller_provider.dart, mobile/lib/features/sellers/data/repositories/seller_repository.dart, mobile/lib/features/auth/presentation/screens/register_screen.dart, mobile/lib/app.dart</files>
  <read_first>
    - mobile/lib/features/sellers/presentation/screens/stripe_onboard_screen.dart (full file, post-Plan-02 state — exact analog for the new screen)
    - mobile/lib/features/sellers/providers/seller_provider.dart (existing methods loadProfile, loadStripeStatus, startStripeOnboarding — analog for startIdentityVerification)
    - mobile/lib/features/sellers/data/repositories/seller_repository.dart (existing startStripeOnboarding wire-up — analog for the new repo method)
    - mobile/lib/features/auth/presentation/screens/register_screen.dart:1-300 (current account-type selector at lines 250-276, _AccountTypeSelector widget shape)
    - mobile/lib/features/auth/providers/auth_provider.dart (or equivalent) — find the existing register() signature to extend with `ein` parameter
    - mobile/lib/app.dart (route registration patterns; identify where /seller/* routes live)
    - mobile/lib/shared/widgets/app_input_field.dart (input field primitive used in register_screen)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md sections "mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart (NEW screen)" + "mobile/lib/features/auth/presentation/screens/register_screen.dart (business toggle reveals EIN)"
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Pitfall 1" (flutter_stripe Identity not supported — use url_launcher)
  </read_first>
  <action>
    1. Create `mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart` by copying the structure of `stripe_onboard_screen.dart` (post-Plan-02 state with WidgetsBindingObserver baked in). Per PATTERNS.md "identity_verify_screen.dart (NEW screen)" section:
       - `class IdentityVerifyScreen extends ConsumerStatefulWidget`
       - State class `with WidgetsBindingObserver`, full lifecycle (initState/dispose/didChangeAppLifecycleState) refetching `loadProfile()` on resumed.
       - Single primary CTA gradient button "Verify ID" → calls `ref.read(sellerProfileProvider.notifier).startIdentityVerification()` → on result, `await launchUrl(Uri.parse(result.url), mode: LaunchMode.externalApplication)`.
       - Show seller's current `idVerified` status from the provider (when true, show a success state instead of the CTA).
       - Use shared widgets: `StyledAppBar`, `SectionCard`, `GradientButton` (verify these exist; the file headers in stripe_onboard_screen.dart will show the imports).
    2. Extend `seller_provider.dart`:
       - Add a method `startIdentityVerification()` returning a result with `url` (and `sessionId`). Mirror `startStripeOnboarding()` line-for-line.
    3. Extend `seller_repository.dart`:
       - Add `startIdentityVerification()` calling `dio.post('/sellers/identity/verify')`. Mirror existing repo methods. Define a small result model class (e.g., `IdentityVerificationResult { final String sessionId; final String url; }`) co-located with the repo or in `data/models/`.
    4. Register the route in `mobile/lib/app.dart`:
       - Add `'/seller/identity/verify'` GoRoute pointing to `IdentityVerifyScreen`. Place near the existing `/seller/stripe-onboard` and `/seller/earnings` routes.
       - Wire a menu entry into the seller-mode area of profile_screen.dart OR into the existing seller_profile screen — minimum-viable wiring is fine; the goal is reachability. If unclear which surface, add a tile to the existing seller settings/verification area; if no obvious analog exists, add a "Verify ID" tile to `profile_screen.dart` directly under the existing Earnings Dashboard entry (only when `appMode == AppMode.seller`).
    5. Extend `register_screen.dart` per PATTERNS.md:
       - Add a `_einController = TextEditingController()` (and dispose it in `dispose()`).
       - Add `'business'` to the `_AccountTypeSelector` options (or wherever the enum mirror lives client-side). The literal must be exactly the quoted string `'business'` so the W5-tightened acceptance grep hits.
       - Conditional reveal: when `_accountType == 'business'`, render an `AppInputField` after the AccountTypeSelector with label `'EIN'`, hint `'12-3456789'`, prefix icon `Icons.business_outlined`, and a validator that requires the field to be non-empty AND match `RegExp(r'^\d{2}-\d{7}$')`.
       - In `_handleRegister()` (or whatever the submit handler is named), pass `ein: _accountType == 'business' ? _einController.text.trim() : null` into the auth provider's register call.
       - Update the auth provider/repo `register()` signatures to accept the optional `ein` parameter, mirroring the existing `phone` / `locationZip` optional pattern.
    6. Run `cd mobile && flutter analyze lib/features/sellers/presentation/screens/identity_verify_screen.dart lib/features/sellers/providers/seller_provider.dart lib/features/sellers/data/repositories/seller_repository.dart lib/features/auth/presentation/screens/register_screen.dart lib/app.dart` — must pass with no errors.
    7. Run `cd mobile && flutter analyze` (full project) — must pass with no errors. (This catches missed import propagation in the auth provider.)
  </action>
  <acceptance_criteria>
    - `test -f mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart` succeeds
    - `grep -c "with WidgetsBindingObserver" mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart` returns `1`
    - `grep -c "launchUrl\|url_launcher" mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart` returns `>=1`
    - `grep -c "AppLifecycleState.resumed" mobile/lib/features/sellers/presentation/screens/identity_verify_screen.dart` returns `1`
    - `grep -c "startIdentityVerification" mobile/lib/features/sellers/providers/seller_provider.dart` returns `>=1`
    - `grep -c "/sellers/identity/verify\|sellers/identity/verify" mobile/lib/features/sellers/data/repositories/seller_repository.dart` returns `>=1`
    - `grep -c "/seller/identity/verify" mobile/lib/app.dart` returns `>=1`
    - **W5 (tightened — quoted literal only):** `grep -c "'business'" mobile/lib/features/auth/presentation/screens/register_screen.dart` returns `>=1`
    - `grep -c "_einController\|einController\|EIN" mobile/lib/features/auth/presentation/screens/register_screen.dart` returns `>=2`
    - `grep -c "RegExp(r'\\^\\\\d{2}-\\\\d{7}\\$')\|^\\\\d{2}-\\\\d{7}\\$" mobile/lib/features/auth/presentation/screens/register_screen.dart` returns `>=1`
    - `cd mobile && flutter analyze` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze</automated>
  </verify>
  <done>identity_verify_screen.dart created with WidgetsBindingObserver + url_launcher hosted flow; provider + repo extended with startIdentityVerification; route registered; register form reveals EIN input on business (literal 'business' string present); full flutter analyze clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /sellers/identity/verify | Authenticated; rate-limited; metadata-tagged for webhook routing |
| Stripe → POST /payments/webhook | Signature-verified via existing STRIPE_WEBHOOK_SECRET; deduped by event.id |
| client → POST /auth/register | EIN format validated server-side (regex) AND client-side (registration form) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Spoofing | Forged Identity webhook | mitigate | Existing `verifyWebhookSignature()` in payments.webhook.ts (single STRIPE_WEBHOOK_SECRET, signature-per-endpoint guarantee per Pitfall 2) |
| T-03-06-02 | Tampering | Replay of identity.verified event | mitigate | Existing `stripe:event:{event.id}` SETNX dedup with 24h TTL — Identity events flow through identical path, no new dedup logic needed |
| T-03-06-03 | Tampering | Client-supplied sellerProfileId routing wrong badge | mitigate | metadata.sellerProfileId is set server-side at session creation (not by client); webhook trusts metadata from authoritative Stripe payload |
| T-03-06-04 | Information Disclosure | EIN echoed in API response | mitigate | EIN never returned in any auth response body or seller response — server-side only; documented in SUMMARY |
| T-03-06-05 | Tampering | Forged EIN format passing validation | accept | Format check is structural only (regex); IRS validation is out of scope; admin can revoke badge if EIN proves invalid |
| T-03-06-06 | Denial of Service | Repeated identity session creation (cost) | mitigate | `idempotencyKey: identity_session_${seller.id}` deduplicates server-side; recommend rate-limit at the route (5/hr per user — matches RESEARCH.md V13) — defer to Plan 08 audit if not added now |
</threat_model>

<verification>
- Backend tests: sellers.test.ts (Identity session + webhook), auth.test.ts (EIN gate), payments.test.ts (regression) all green
- Mobile flutter analyze clean
- Stripe Dashboard runbook documented (manual step)
- Migration directory backend/prisma/migrations/*-add_user_ein/ exists with migration.sql (or documented custom-migration equivalent)
</verification>

<success_criteria>
Phase 3 Success Criterion 5 fully delivered: ID Verified badge auto-awarded via Stripe Identity webhook; EIN required at business registration with format validation. Mobile uses hosted flow (per Pitfall 1). Single endpoint + single secret (per Pitfall 2).
</success_criteria>

<output>
After completion, create `.planning/phases/03-mvp-implementation-closeout/03-06-SUMMARY.md` with:
- Files modified/created (15 paths)
- New endpoint + webhook handler diff summary
- EIN gate diff summary
- Stripe Dashboard runbook step (markdown checkbox list)
- Identity Verify screen + register form diff summary
- Verification command output
- Schema migration choice (Prisma migrate dev vs custom-migrations SQL) + applied migration path on disk
</output>
