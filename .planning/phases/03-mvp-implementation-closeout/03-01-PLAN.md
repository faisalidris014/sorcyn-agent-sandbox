---
phase: 03-mvp-implementation-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/offers/offers.service.ts
autonomous: true
requirements:
  - REQ-max-offers-per-post
must_haves:
  truths:
    - "A post receiving its 26th offer is rejected with HTTP 409 and a maximum-of-25 message"
    - "Posts can accumulate up to 25 distinct offers before the cap engages"
    - "saved-sellers, archive endpoint, video MIMEs, and counter-offer chain shipped surfaces remain unmodified by this plan (verified, not rebuilt)"
  artifacts:
    - path: "backend/src/modules/offers/offers.service.ts"
      provides: "MAX_OFFERS_PER_POST = 25 constant"
      contains: "const MAX_OFFERS_PER_POST = 25"
  key_links:
    - from: "backend/src/modules/offers/offers.service.ts (submitOffer)"
      to: "MAX_OFFERS_PER_POST constant"
      via: "postOfferCount >= MAX_OFFERS_PER_POST guard at line ~45"
      pattern: "postOfferCount >= MAX_OFFERS_PER_POST"
---

<objective>
Reconcile the `MAX_OFFERS_PER_POST` constant from the audit-drift value of 10 to the ADR-locked value of 25 so buyers can receive up to 25 distinct seller offers per post (per CLAUDE.md docs/decisions §Offers and ADR aggregate). Behavioral change: buyers who currently cannot receive offers 11–25 will start receiving them; no data migration needed.

This plan is a single-file constant change. Verification of other already-shipped audit-gap surfaces (saved-sellers module, archived PostStatus, video MIME allowlist, counter-offer chain, geocoding utility) lives in the Vitest closeout audit suite (Plan 08), NOT here.

Purpose: Close the audit-drift gap that puts the codebase in violation of an ADR-locked decision.
Output: Single-line constant change + automated test that the 26th offer rejects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mvp-implementation-closeout/03-CONTEXT.md
@.planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md
@.planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md
@backend/src/modules/offers/offers.service.ts

<interfaces>
From backend/src/modules/offers/offers.service.ts:
```typescript
const MAX_OFFERS_PER_POST = 10; // line 21 — TARGET FOR CHANGE
// Used at line 45-46:
if (postOfferCount >= MAX_OFFERS_PER_POST) {
  throw new ConflictError(`This post has reached the maximum of ${MAX_OFFERS_PER_POST} offers`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump MAX_OFFERS_PER_POST from 10 to 25</name>
  <files>backend/src/modules/offers/offers.service.ts, backend/tests/offers.test.ts</files>
  <read_first>
    - backend/src/modules/offers/offers.service.ts:1-100 (file being modified — confirm constant at line 21 and guard at line 45-46)
    - backend/tests/offers.test.ts (existing offer test patterns; check for any test asserting cap=10)
    - .planning/phases/03-mvp-implementation-closeout/03-PATTERNS.md (sections "backend/src/modules/offers/offers.service.ts (line 21 const + counterOffer depth-cap)" and "Shared Patterns")
    - .planning/phases/03-mvp-implementation-closeout/03-RESEARCH.md "Recommendations for Planner Plan 1" + "Common Pitfalls 3" (verify before changing)
  </read_first>
  <behavior>
    - Test 1: After change, the constant `MAX_OFFERS_PER_POST` equals 25.
    - Test 2: Submitting offer #26 on a post that already has 25 active offers from distinct sellers throws ConflictError with message containing "maximum of 25 offers".
    - Test 3: Submitting offer #25 on a post that has 24 active offers succeeds (returns 201).
    - Test 4: Any prior test asserting cap=10 must be updated to cap=25; if no such test exists, add a new behavioral test that creates 25 offers and asserts the 26th 409s.
  </behavior>
  <action>
    1. Run `grep -rn "MAX_OFFERS_PER_POST\|reached the maximum of 10 offers\|maximum of 10" backend/` to enumerate all references.
    2. Edit `backend/src/modules/offers/offers.service.ts` line 21 from `const MAX_OFFERS_PER_POST = 10;` to `const MAX_OFFERS_PER_POST = 25;` — keep exact spacing/formatting; no comment additions.
    3. Search backend/tests/offers.test.ts for any literal `10` or `"maximum of 10"` tied to the offer cap. If found, update to `25` / `"maximum of 25"`.
    4. Add (or extend an existing describe block in) `backend/tests/offers.test.ts` a behavioral test:
       - Create one buyer post.
       - Create 25 distinct seller users (loop with `createTestUser` from `backend/tests/helpers.ts`).
       - Submit one offer per seller via `app.inject({ method: 'POST', url: '/api/v1/offers', headers: authHeaders(seller.token), payload: { postId, price: 100, ... } })`. Assert each returns 201.
       - Create a 26th seller; submit; assert `res.statusCode === 409` and `JSON.stringify(res.json()).includes('maximum of 25 offers')`.
    5. Run `npx vitest run tests/offers.test.ts` and confirm green.
    6. Do NOT modify any other files. Do NOT touch the saved-sellers / archive / video-MIME / counter-offer surfaces (those live in Plan 08 audit assertions).
  </action>
  <acceptance_criteria>
    - `grep -c "MAX_OFFERS_PER_POST = 25" backend/src/modules/offers/offers.service.ts` returns `1`
    - `grep -c "MAX_OFFERS_PER_POST = 10" backend/src/modules/offers/offers.service.ts` returns `0`
    - `grep -v '^\s*//' backend/src/modules/offers/offers.service.ts | grep -c "maximum of 10 offers"` returns `0`
    - The added behavioral test in `backend/tests/offers.test.ts` includes the literal string `'maximum of 25 offers'` in an assertion
    - `npx vitest run tests/offers.test.ts` exits with code 0
    - Files touched in this plan: exactly `backend/src/modules/offers/offers.service.ts` and `backend/tests/offers.test.ts` — `git diff --name-only` returns those two paths only
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run tests/offers.test.ts</automated>
  </verify>
  <done>MAX_OFFERS_PER_POST=25 lands at line 21; behavioral test asserts 26th-offer 409 with "maximum of 25 offers" message; no other files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/offers (POST) | Untrusted seller submits offer; cap enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | offers.service.submitOffer | mitigate | MAX_OFFERS_PER_POST=25 hard cap prevents unbounded offers per post; rejected with 409 ConflictError |
| T-03-01-02 | Tampering | client-supplied offer count | accept | Cap is enforced server-side via `prisma.offer.count` (line ~37) — client cannot bypass |
</threat_model>

<verification>
- `grep -n "MAX_OFFERS_PER_POST" backend/src/modules/offers/offers.service.ts` shows exactly the constant declaration + 1 usage in the guard, both at value 25
- Full offers test file passes
- No regressions in adjacent tests: `cd backend && npx vitest run tests/offers.test.ts tests/posts.test.ts` exits 0
</verification>

<success_criteria>
Phase 3 Success Criterion 3 partially advanced: `MAX_OFFERS_PER_POST` is 25 (one of the seven assertions the audit re-run will verify in Plan 08).
</success_criteria>

<output>
After completion, create `.planning/phases/03-mvp-implementation-closeout/03-01-SUMMARY.md` with:
- Constant value before/after
- Tests added/modified
- Files modified (exact paths from `git diff --name-only`)
- Verification command output
</output>
